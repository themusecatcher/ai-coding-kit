---
description: 验证行为详细规范（编辑工具假性成功兜底、批量重命名三大陷阱）。编辑/重命名/全局替换场景时按需加载。
alwaysApply: false
enabled: true
updatedAt: 2026-05-26T14:17:00.000Z
provider: 
---

# 验证行为详细规范

> 从 AI 行为规范精简版移出（2026-05-26）。基线规则见 AI 行为规范 §「验证行为规范」。

## 跨文件交叉引用必须双向闭环（2026-05-15）

设计原则文档 / 规则文档 / 实施沉淀文档之间，引用必须**双向闭环**而非单向。判定标准：对每条新增的「A 文档引用 B 文档」，必须同时检查「B 文档是否引用回 A 文档」；自检命令：在 A、B 双方各自 `grep` 对方的关键标识符，若任一方为空 → 断链待修。

## 编辑工具假性成功兜底（2026-05-19 ×4 同形 promote）

编辑工具（`replace_in_file` / `multi_replace` / `edit_file`）回报 success 不等于内容真实落盘；`grep_search` / `codebase_search` 工具结果存在缓存延迟，不能作为承诺一致性的最终证据。

**同形病灶累积**：
1. `replace_in_file` 报 success 但磁盘未变（5/15）
2. `multi_replace` 越权重写整个文件（5/19 早）
3. `multi_replace` 含中文标点 old_string 偶发匹配失败（5/19 晚）
4. `grep_search` 显示零命中但终端 grep 实测全命中（5/19 晚）

**强制范式**：
1. 关键编辑后立即 `grep -c "<新增关键词>" <文件>` + `wc -l <文件>` 双重验证
2. 工具失败 1 次后立即降级换工具（`multi_replace` → `replace_in_file` → `edit_file` → 终端 `printf >> tmp; mv -f`），禁止重试同工具同参数
3. 含中文标点（`（）`/`、`/`""`/`「」`）的 old_string 优先用 `edit_file` 占位符模式

## 批量重命名/全局替换三大陷阱（2026-05-21 ×5 同形 promote）

批量替换/重命名场景中，连环 bash 命令存在三类独立陷阱常导致**假性成功**：

### ① 链式 sed + `[[ ]] && mv` 短路判断

一行 bash 串多个 sed + grep + 条件判断 + mv 时，终端回显被截断 + 短路链使 mv 静默跳过，但前置 echo "✅" 仍执行 → 误判落盘。

**强制**：改用 `sed -i ''`（macOS 语法）单文件单行命令。

### ② `grep -c` 子串污染

`grep -c "issue-trace"` 同时匹配 "issues-trace"（子串），`DIFF_AFTER` 报告值不可靠。

**强制**：只看残留旧名命中数（应为 0），不依赖新名命中数。

### ③ `wc -l` 终端渲染伪影

`wc -l < file.txt` 报告非 0 数字但 `cat` 显示空文件，是终端缓冲区残影。

**强制**：用 `ls -la` 看 size 字节数 + `hexdump -C file | head` 二进制视图 + `file file` 三重物理事实兜底。

### SOP

备份 → 双向扫描原始引用点（旧名 + 新名兜底）→ sed -i '' 单文件单行替换 → ls/hexdump 物理验证 → 双向 grep 实测（旧 0 + 新 N）→ 跑回归。

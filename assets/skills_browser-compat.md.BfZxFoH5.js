import{_ as d,C as c,o as a,c as l,a2 as s,b as r,w as o,a as n,E as i,a3 as p}from"./chunks/framework.BUwdTBzS.js";const A=JSON.parse('{"title":"browser-compat","description":"|","frontmatter":{"title":"browser-compat","description":"|"},"headers":[],"relativePath":"skills/browser-compat.md","filePath":"skills/browser-compat.md","lastUpdated":null}'),h={name:"skills/browser-compat.md"};function u(m,e,b,k,E,g){const t=c("Mermaid");return a(),l("div",null,[e[1]||(e[1]=s('<blockquote><p>📄 本页由源文件 <code>skills/browser-compat/SKILL.md</code> 自动投影生成（单一权威源）。请勿直接编辑本页。</p></blockquote><h1 id="browser-compat" tabindex="-1">browser-compat <a class="header-anchor" href="#browser-compat" aria-label="Permalink to &quot;browser-compat&quot;">​</a></h1><blockquote><p>统一的浏览器兼容性检查入口。规则文件 <code>~/.codebuddy/rules/浏览器兼容性规范.mdc</code> 为<strong>单一真相源</strong>，本 skill 只做加载 + 路由 + 执行。</p></blockquote><hr><h2 id="使用方式" tabindex="-1">使用方式 <a class="header-anchor" href="#使用方式" aria-label="Permalink to &quot;使用方式&quot;">​</a></h2><h3 id="独立使用-用户主动触发" tabindex="-1">独立使用（用户主动触发） <a class="header-anchor" href="#独立使用-用户主动触发" aria-label="Permalink to &quot;独立使用（用户主动触发）&quot;">​</a></h3><p>用户说&quot;检查一下兼容性&quot;/&quot;这段代码能用吗&quot;/&quot;扫描一下 diff 兼容性&quot;时触发。</p><ul><li>默认对当前 <code>git diff</code> 执行扫描</li><li>可指定文件：<code>use_skill(&#39;browser-compat&#39;)</code> + 「帮我看 <code>src/xxx.tsx</code>」</li><li>可指定目录/全仓：「扫全项目」「升级 browserslist 前看看」</li></ul><p>→ 加载 <code>modules/quick-scan.md</code></p><h3 id="被-dev-flow-code-review-调用-被动触发" tabindex="-1">被 dev-flow / code-review 调用（被动触发） <a class="header-anchor" href="#被-dev-flow-code-review-调用-被动触发" aria-label="Permalink to &quot;被 dev-flow / code-review 调用（被动触发）&quot;">​</a></h3><ul><li><code>code-review</code> L1 审查第 5 条：有 JS/CSS 改动时代理调用本 skill</li><li>dev-flow 步骤 5 / 5.5 / 6：按 <code>shared-rules.md §5</code> 的 Skill 匹配表自动发现</li></ul><p>→ 加载 <code>modules/dev-flow-hook.md</code></p><hr><h2 id="核心规则来源-单一真相" tabindex="-1">核心规则来源（单一真相） <a class="header-anchor" href="#核心规则来源-单一真相" aria-label="Permalink to &quot;核心规则来源（单一真相）&quot;">​</a></h2><p><strong>请勿在本 skill 任何文件中重复列禁用清单</strong>。规则只有一份：</p><p>📄 <code>~/.codebuddy/rules/浏览器兼容性规范.mdc</code></p><p>该规则是 <code>alwaysApply: false</code>，按需加载。本 skill 被调用时必须先 <code>read_rules</code> 或 <code>read_file</code> 加载该规则作为检查依据。</p><p><strong>双向同步由元门控守护</strong>：<code>scripts/meta/validate-rules-sync.sh</code> 校验规则文件 ↔ <code>scripts/compat-check.js</code> 的 JS_RULES/CSS_RULES 集合双向一致（包含 18 条规则的关键字落地检查）。任何一边变动必须同步另一边，否则元门控失败 exit 1。临时绕过：<code>SKIP_COMPAT_RULES_SYNC=1 bash validate-rules-sync.sh</code>。</p><hr><h2 id="按需加载索引" tabindex="-1">按需加载索引 <a class="header-anchor" href="#按需加载索引" aria-label="Permalink to &quot;按需加载索引&quot;">​</a></h2><table tabindex="0"><thead><tr><th>场景</th><th>加载文件</th></tr></thead><tbody><tr><td>用户主动扫描（独立使用）</td><td><code>modules/quick-scan.md</code></td></tr><tr><td>被 dev-flow / code-review 调用</td><td><code>modules/dev-flow-hook.md</code></td></tr><tr><td>违规后查修复方案</td><td><code>references/fix-patterns.md</code></td></tr><tr><td>多项目基线对照 / 判定当前项目基线</td><td><code>references/browser-baseline.md</code></td></tr><tr><td>排查误报 / 豁免方法 / 性能边界</td><td><code>references/known-false-positives.md</code></td></tr><tr><td>初始化项目级配置</td><td><code>templates/project-config.json</code></td></tr><tr><td>输出格式契约（JSON Schema）</td><td><code>schemas/finding.schema.json</code></td></tr><tr><td>批量扫描（脚本执行）</td><td><code>scripts/compat-check.js</code></td></tr><tr><td>规则同步元门控</td><td><code>scripts/meta/validate-rules-sync.sh</code></td></tr><tr><td>配置文件 IDE 校验 schema</td><td><code>schema.json</code></td></tr></tbody></table><hr><h2 id="执行流程-通用" tabindex="-1">执行流程（通用） <a class="header-anchor" href="#执行流程-通用" aria-label="Permalink to &quot;执行流程（通用）&quot;">​</a></h2>',23)),(a(),r(p,null,{default:o(()=>[i(t,{id:"mermaid-179",class:"mermaid-diagram",graph:"graph%20TD%0A%20%20%20%20A%5B%E8%A7%A6%E5%8F%91%5D%20--%3E%20B%5B%E5%8A%A0%E8%BD%BD%E8%A7%84%E5%88%99%E6%96%87%E4%BB%B6%5D%0A%20%20%20%20B%20--%3E%20C%5B%E8%84%9A%E6%9C%AC%E6%89%A7%E8%A1%8C%20load-config.js%5D%0A%20%20%20%20C%20--%3E%20D%5B%E8%BE%93%E5%87%BA%20baseline%20%2B%20polyfills%20%2B%20ignore%5D%0A%20%20%20%20D%20--%3E%20E%5B%E8%84%9A%E6%9C%AC%E6%89%A7%E8%A1%8C%20rule-filter.js%5D%0A%20%20%20%20E%20--%3E%20F%5B%E6%89%AB%E6%8F%8F%E4%BB%A3%E7%A0%81%EF%BC%88compat-check.js%EF%BC%89%5D%0A%20%20%20%20F%20--%3E%20G%7B%E5%8F%91%E7%8E%B0%E8%BF%9D%E8%A7%84%3F%7D%0A%20%20%20%20G%20--%3E%7C%E6%97%A0%7C%20H%5B%E2%9C%85%20%E9%80%9A%E8%BF%87%5D%0A%20%20%20%20G%20--%3E%7C%F0%9F%94%B4%20CRITICAL%7C%20I%5BBlock%20%E5%B9%B6%E6%8F%90%E7%A4%BA%E4%BF%AE%E5%A4%8D%5D%0A%20%20%20%20G%20--%3E%7C%F0%9F%9F%A1%20WARNING%7C%20J%5B%E5%BC%B9%E5%87%BA%E7%94%A8%E6%88%B7%E5%86%B3%E7%AD%96%5D%0A%20%20%20%20I%20--%3E%20K%5B%E5%8A%A0%E8%BD%BD%20fix-patterns.md%20%E6%8F%90%E4%BE%9B%E6%9B%BF%E4%BB%A3%E6%96%B9%E6%A1%88%5D%0A%20%20%20%20J%20--%3E%20K%0A"})]),fallback:o(()=>[...e[0]||(e[0]=[n(" Loading... ",-1)])]),_:1})),e[2]||(e[2]=s(`<blockquote><p>基线判定 / 规则过滤 / 路径过滤均由脚本执行，不依赖 AI 判断。LLM 仅负责： 1）判断违规是否为实隟误报（2）选择修复方案（3）与用户交互决策。</p></blockquote><hr><h2 id="配置文件-schema-兼容" tabindex="-1">配置文件 Schema 兼容 <a class="header-anchor" href="#配置文件-schema-兼容" aria-label="Permalink to &quot;配置文件 Schema 兼容&quot;">​</a></h2><p><code>load-config.js</code> 同时识别两套 <code>.browser-compat.json</code> schema 风格，用户既有项目零改动即可使用。新项目推荐使用「我们 schema」（命名更清晰），存量项目沿用「用户 schema」（已在 my-project / project-a / my-site 验证）。</p><h3 id="字段对照表" tabindex="-1">字段对照表 <a class="header-anchor" href="#字段对照表" aria-label="Permalink to &quot;字段对照表&quot;">​</a></h3><table tabindex="0"><thead><tr><th>用途</th><th>用户 schema（主路径）</th><th>我们 schema（语法糖）</th><th>优先级</th></tr></thead><tbody><tr><td>基线版本</td><td><code>targets: { chrome: &quot;70&quot; }</code> 复数 + 字符串</td><td><code>target: &quot;conservative&quot;</code> + <code>baseline: { chrome: 70 }</code> 数字</td><td><code>target</code> &gt; <code>targets</code> &gt; <code>baseline</code></td></tr><tr><td>规则开关</td><td><code>rules: { &quot;X&quot;: &quot;error|warn|info|off&quot; }</code> ESLint 风格</td><td><code>ignore: [{ rule_id, reason }]</code> 显式对象</td><td><code>ignore</code> 对象数组优先；<code>rules.off</code> 合并补充</td></tr><tr><td>路径排除</td><td><code>ignore: [&quot;node_modules/**&quot;]</code> 字符串数组</td><td><code>excludePaths: [...]</code> 显式</td><td><code>excludePaths</code> 优先；缺省时嗅探 <code>ignore</code> 第一个元素类型</td></tr><tr><td>多项目</td><td><code>projects: { &quot;&lt;name&gt;&quot;: {...} }</code></td><td>—</td><td>通过 <code>package.json&gt;name</code> 反查；找不到再降级到目录名</td></tr></tbody></table><h3 id="ignore-字段的多态嗅探-关键" tabindex="-1"><code>ignore</code> 字段的多态嗅探（关键） <a class="header-anchor" href="#ignore-字段的多态嗅探-关键" aria-label="Permalink to &quot;\`ignore\` 字段的多态嗅探（关键）&quot;">​</a></h3><p><code>ignore</code> 字段在两套 schema 里<strong>字段名相同但语义不同</strong>，脚本通过第一个元素的类型自动判定：</p><ul><li>第一个元素是 <code>string</code> → 用户 schema：路径 glob 数组 → 合并到 <code>excludePaths</code></li><li>第一个元素是 <code>object</code>（含 <code>rule_id</code>）→ 我们 schema：规则豁免数组 → 写入 <code>ignore</code></li><li>空数组 → 不动作</li><li>类型异常 → 写 stderr 警告并跳过（不静默失败）</li></ul><h3 id="加载优先级链" tabindex="-1">加载优先级链 <a class="header-anchor" href="#加载优先级链" aria-label="Permalink to &quot;加载优先级链&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>.browser-compat.json</span></span>
<span class="line"><span>  ├─ 含 projects 字段 → 反查 package.json&gt;name → 用对应子配置</span></span>
<span class="line"><span>  ├─ 含 target/targets/baseline → 直接使用</span></span>
<span class="line"><span>  └─ 都没有 → fallback 到 ↓</span></span>
<span class="line"><span>package.json&gt;browserslist</span></span>
<span class="line"><span>  └─ 缺失 → fallback 到 ↓</span></span>
<span class="line"><span>.browserslistrc</span></span>
<span class="line"><span>  └─ 缺失 → 默认保守型（chrome:70 / safari:12 / firefox:68 / edge:79）</span></span></code></pre></div><blockquote><p>关键修复（2026-05-15）：当 <code>.browser-compat.json</code> 存在但所有 baseline 字段都缺失时，旧版本会提前 <code>return</code> 不再 fallback，导致 source 错标为 <code>fallback</code>。新版本会继续走 browserslist 优先级链，并在 source 字段标记 <code>package.json&gt;browserslist (compat-json fallback)</code> 便于追溯。</p></blockquote><h3 id="ide-校验配置-推荐" tabindex="-1">IDE 校验配置（推荐） <a class="header-anchor" href="#ide-校验配置-推荐" aria-label="Permalink to &quot;IDE 校验配置（推荐）&quot;">​</a></h3><p>在项目的 <code>.browser-compat.json</code> 第一行添加 <code>$schema</code> 引用，IDE 会提供自动补全和字段校验：</p><div class="language-jsonc vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">jsonc</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;$schema&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;.codebuddy/skills/browser-compat/schema.json&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // ...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h3 id="脚本暴露的内部工具-测试与自检用" tabindex="-1">脚本暴露的内部工具（测试与自检用） <a class="header-anchor" href="#脚本暴露的内部工具-测试与自检用" aria-label="Permalink to &quot;脚本暴露的内部工具（测试与自检用）&quot;">​</a></h3><p><code>load-config.js</code> 的 <code>_internal</code> 命名空间暴露给测试：<code>normalizeMajorVersion / targetsToMinVersions / rulesToIgnoreAndCustom / classifyIgnoreField / resolveCentralProject</code>。生产代码请只用 <code>loadConfig()</code>。</p><hr><h2 id="与其他-skill-的协作关系" tabindex="-1">与其他 Skill 的协作关系 <a class="header-anchor" href="#与其他-skill-的协作关系" aria-label="Permalink to &quot;与其他 Skill 的协作关系&quot;">​</a></h2><table tabindex="0"><thead><tr><th>关联 Skill</th><th>协作关系</th></tr></thead><tbody><tr><td><code>code-review</code></td><td>L1 第 5 条代理调用本 skill，本 skill 输出回填到 code-review 报告</td></tr><tr><td><code>browser-toolkit</code></td><td>跨浏览器实测（V4 Browser 阶段）时本 skill 提供&quot;该测什么版本&quot;的信息，<code>browser-toolkit</code> 负责实际测试</td></tr><tr><td><code>dev-flow</code></td><td>按 <code>shared-rules.md §5</code> 匹配表在步骤 5/5.5/6 自动被发现</td></tr></tbody></table><hr><h2 id="输出规范" tabindex="-1">输出规范 <a class="header-anchor" href="#输出规范" aria-label="Permalink to &quot;输出规范&quot;">​</a></h2><p>所有报告的文件路径引用必须遵循 <code>~/.codebuddy/rules/AI行为规范.mdc</code> 的「文件/代码位置引用」规范（反引号包裹相对路径 + <code>L行号</code>）。</p><p>单项违规输出格式：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[🔴 CRITICAL] 使用了 :has() 选择器</span></span>
<span class="line"><span>文件: \`src/components/Foo.tsx\` L42</span></span>
<span class="line"><span>问题: 项目基线 Safari 12+ 不支持 :has()，将导致样式失效</span></span>
<span class="line"><span>修复: 改用 JS 逻辑 + className 切换</span></span>
<span class="line"><span>参考: references/fix-patterns.md §CSS:has()替代方案</span></span></code></pre></div><p>Summary 表格：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>| 严重度 | 数量 | 状态 |</span></span>
<span class="line"><span>|--------|------|------|</span></span>
<span class="line"><span>| 🔴 CRITICAL | 0 | ✅ 通过 |</span></span>
<span class="line"><span>| 🟡 WARNING | 2 | ⚠️ 需确认 |</span></span>
<span class="line"><span></span></span>
<span class="line"><span>结论: ⚠️ 2 个 WARNING 需决策；0 个 CRITICAL。</span></span></code></pre></div>`,27))])}const q=d(h,[["render",u]]);export{A as __pageData,q as default};

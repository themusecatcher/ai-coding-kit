import{_ as e,o,c as d,a2 as l}from"./chunks/framework.BUwdTBzS.js";const p=JSON.parse('{"title":"dev-comp","description":"面向 vue-amazing-ui 组件库的单组件开发迭代工作流。轻量领域流程（6 阶段）+ 软交付前精修与三方一致性收口（品牌信息清除 / 注释精修 / Props 排序 / 演示用例排序布局 / docs↔views↔源码对账，兼容分批提交）+ 软复用 dev-flow 生态能力（工作上下文/p…","frontmatter":{"title":"dev-comp","description":"面向 vue-amazing-ui 组件库的单组件开发迭代工作流。轻量领域流程（6 阶段）+ 软交付前精修与三方一致性收口（品牌信息清除 / 注释精修 / Props 排序 / 演示用例排序布局 / docs↔views↔源码对账，兼容分批提交）+ 软复用 dev-flow 生态能力（工作上下文/p…"},"headers":[],"relativePath":"skills/dev-comp.md","filePath":"skills/dev-comp.md","lastUpdated":null}'),s={name:"skills/dev-comp.md"};function n(c,t,r,a,i,g){return o(),d("div",null,[...t[0]||(t[0]=[l(`<blockquote><p>📄 本页由源文件 <code>skills/dev-comp/SKILL.md</code> 自动投影生成（单一权威源）。请勿直接编辑本页。</p></blockquote><h1 id="dev-comp" tabindex="-1">dev-comp <a class="header-anchor" href="#dev-comp" aria-label="Permalink to &quot;dev-comp&quot;">​</a></h1><blockquote><p>定位：vue-amazing-ui 单组件开发迭代领域 SOP。 架构：<strong>轻量领域流程 + 软复用 dev-flow 能力模块</strong>。全程❌ 不产生 <code>.flow</code> 锁、❌ 不走 dev-flow 重型门控（<code>.validated</code> 物理检查点 / JSON 逐步校验 / 门控 subagent / post-step 脚本 / 工具门禁）；✅ 仅保留<strong>轻量交互式 Gate</strong>——每阶段完成后输出「阶段完成报告」并弹 <code>ask_followup_question</code>，等用户确认再进入下一阶段（详见「6 阶段 + Gate 流程总览」）；✅ 另配 1 个<strong>轻量校验脚本</strong> <code>scripts/validate-component.sh</code> 承载 Gate 5 确定性检查（A/B/C/E/F/G 配置项 + S 提交红线，设计哲学「确定性用代码」，详见「能力复用索引」）。</p></blockquote><h2 id="⚙️-个人化配置区-复用-分享时只改这一块" tabindex="-1">⚙️ 个人化配置区（复用/分享时只改这一块） <a class="header-anchor" href="#⚙️-个人化配置区-复用-分享时只改这一块" aria-label="Permalink to &quot;⚙️ 个人化配置区（复用/分享时只改这一块）&quot;">​</a></h2><blockquote><p>本 skill 核心流程通用；下列配置<strong>换人换机时只需填 4 个本地路径</strong>，其余保持默认即可。</p></blockquote><h3 id="必填-首次使用前填写-artifacts-fallback-dir-可留空" tabindex="-1">必填（首次使用前填写；<code>ARTIFACTS_FALLBACK_DIR</code> 可留空） <a class="header-anchor" href="#必填-首次使用前填写-artifacts-fallback-dir-可留空" aria-label="Permalink to &quot;必填（首次使用前填写；\`ARTIFACTS_FALLBACK_DIR\` 可留空）&quot;">​</a></h3><table tabindex="0"><thead><tr><th style="text-align:left;">配置项</th><th style="text-align:left;">填写你的本地路径</th><th style="text-align:left;">说明</th></tr></thead><tbody><tr><td style="text-align:left;"><code>PROJECT_ROOT</code></td><td style="text-align:left;"><code>~/myGithub/vue-amazing-ui</code></td><td style="text-align:left;">组件库项目根</td></tr><tr><td style="text-align:left;"><code>REF_ANTDV_LOCAL</code></td><td style="text-align:left;"><code>~/myGithub/ant-design-vue</code></td><td style="text-align:left;">Ant Design Vue 本地 clone 路径（留空则降级用官网）</td></tr><tr><td style="text-align:left;"><code>REF_NAIVE_LOCAL</code></td><td style="text-align:left;"><code>~/myGithub/naive-ui</code></td><td style="text-align:left;">Naive UI 本地 clone 路径（留空则降级用官网）</td></tr><tr><td style="text-align:left;"><code>ARTIFACTS_FALLBACK_DIR</code></td><td style="text-align:left;"><code>~/myGithub/ai-coding-kit/skills/dev-comp/artifacts/</code></td><td style="text-align:left;">产物归档兜底目录（用户将产物归档到 skill 下时使用；留空则仅扫 <code>~/.codebuddy/</code> 运行时目录）</td></tr></tbody></table><h3 id="默认可用-无需修改" tabindex="-1">默认可用（无需修改） <a class="header-anchor" href="#默认可用-无需修改" aria-label="Permalink to &quot;默认可用（无需修改）&quot;">​</a></h3><table tabindex="0"><thead><tr><th style="text-align:left;">配置项</th><th style="text-align:left;">当前值</th><th style="text-align:left;">说明</th></tr></thead><tbody><tr><td style="text-align:left;"><code>REF_ANTDV_GH</code></td><td style="text-align:left;"><a href="https://github.com/vueComponent/ant-design-vue" target="_blank" rel="noreferrer">https://github.com/vueComponent/ant-design-vue</a></td><td style="text-align:left;">Ant Design Vue GitHub</td></tr><tr><td style="text-align:left;"><code>REF_NAIVE_GH</code></td><td style="text-align:left;"><a href="https://github.com/tusen-ai/naive-ui" target="_blank" rel="noreferrer">https://github.com/tusen-ai/naive-ui</a></td><td style="text-align:left;">Naive UI GitHub</td></tr><tr><td style="text-align:left;"><code>REF_ANTDV_DOC</code></td><td style="text-align:left;"><a href="https://www.antdv.com/components/overview-cn/" target="_blank" rel="noreferrer">https://www.antdv.com/components/overview-cn/</a></td><td style="text-align:left;">antdv 官网</td></tr><tr><td style="text-align:left;"><code>REF_NAIVE_DOC</code></td><td style="text-align:left;"><a href="https://www.naiveui.com/zh-CN/os-theme/docs/introduction" target="_blank" rel="noreferrer">https://www.naiveui.com/zh-CN/os-theme/docs/introduction</a></td><td style="text-align:left;">naive 官网</td></tr><tr><td style="text-align:left;"><code>CAP_WORKING_CONTEXT_DIR</code></td><td style="text-align:left;"><code>~/.codebuddy/dev-comp/working-context/</code></td><td style="text-align:left;">工作上下文目录（dev-comp 专属）</td></tr><tr><td style="text-align:left;"><code>CAP_METRICS_DIR</code></td><td style="text-align:left;"><code>~/.codebuddy/dev-comp/metrics/</code></td><td style="text-align:left;">度量报告目录（dev-comp 专属）</td></tr></tbody></table><blockquote><p>本地 clone 不存在时：降级为仅用官网（web_fetch）参考，并提示用户可clone 以获得源码级复用。</p></blockquote><h2 id="📋-项目规范权威源-必读-·-2026-09-15-新增" tabindex="-1">📋 项目规范权威源（必读 · 2026-09-15 新增） <a class="header-anchor" href="#📋-项目规范权威源-必读-·-2026-09-15-新增" aria-label="Permalink to &quot;📋 项目规范权威源（必读 · 2026-09-15 新增）&quot;">​</a></h2><blockquote><p>项目内已有面向贡献者的设计规范（<code>{PROJECT_ROOT}/development/</code>），是组件实现的<strong>权威源</strong>；本 skill 只做「引用 + 增量补充」，<strong>禁止另起平行规则</strong>。 ⚠️ 本 skill 的 checklist / 模板 / 脚本与项目规范冲突时，<strong>以项目规范为准</strong>，并<strong>立即回填本 skill</strong>（单向断链是历史欠账的根因，见 <code>rules/按需-Skill设计-跨文件协作.mdc</code> §2.1）。映射表详见 <code>references/project-map.md</code> §项目规范权威源索引。</p></blockquote><table tabindex="0"><thead><tr><th style="text-align:left;">必读文件</th><th style="text-align:left;">关键内容</th><th style="text-align:left;">对应阶段</th></tr></thead><tbody><tr><td style="text-align:left;"><code>development/component-design.md</code></td><td style="text-align:left;">SFC 三段式 / <code>export interface Props</code> / <strong><code>defineSlots</code> + <code>&lt;组件名&gt;Slots</code></strong> / 主题注入 / 样式与 CSS 变量 / <strong>根类名 <code>{组件名}-wrap</code></strong></td><td style="text-align:left;">1-2</td></tr><tr><td style="text-align:left;"><code>development/import-export.md</code></td><td style="text-align:left;">三层导出模型 / 组件 <code>index.ts</code> 三件套 / <strong>新增组件「三步接线」Checklist</strong> / <strong><code>style-deps.ts</code> 四张表</strong>（D 方案单一数据源）</td><td style="text-align:left;">1</td></tr><tr><td style="text-align:left;"><code>development/demo-doc-guide.md</code></td><td style="text-align:left;">演示页结构 / 自动路由 / 组件文档模板 / 侧边栏维护 / <strong>演示与文档必须一致</strong></td><td style="text-align:left;">3-4</td></tr><tr><td style="text-align:left;"><code>development/project-structure.md</code></td><td style="text-align:left;">顶层目录职责 / 命名规范速查</td><td style="text-align:left;">1/3/4</td></tr><tr><td style="text-align:left;"><code>development/build-system.md</code></td><td style="text-align:left;">三产物构建（dist / es / lib）/ 别名与模块解析</td><td style="text-align:left;">5（发布）</td></tr><tr><td style="text-align:left;"><code>types/global-components.d.ts</code></td><td style="text-align:left;"><strong>手工维护</strong>的自有组件全局类型声明 → 新增组件<strong>必登记</strong>（⚠️ 漏登记不报错、<code>pnpm type-check</code> 仍 PASS，见 <code>linkage-map.md</code> §⑮）</td><td style="text-align:left;">1</td></tr></tbody></table><h2 id="📦-产物存储设计原则-2026-08-18-固化-·-2026-08-19-补第-6-条-·-2026-08-21-第-6-条收敛为「统一运行时目录」" tabindex="-1">📦 产物存储设计原则（2026-08-18 固化 · 2026-08-19 补第 6 条 · 2026-08-21 第 6 条收敛为「统一运行时目录」） <a class="header-anchor" href="#📦-产物存储设计原则-2026-08-18-固化-·-2026-08-19-补第-6-条-·-2026-08-21-第-6-条收敛为「统一运行时目录」" aria-label="Permalink to &quot;📦 产物存储设计原则（2026-08-18 固化 · 2026-08-19 补第 6 条 · 2026-08-21 第 6 条收敛为「统一运行时目录」）&quot;">​</a></h2><blockquote><p>决策背景：深入评估过「产物入仓库」方案，被开源仓库 git 污染 + dev-flow 生态路径硬绑定否决。产物<strong>一律</strong>留在 <code>~/.codebuddy/</code>（第 1-5 条 2026-08-18 固化；第 6 条 2026-08-19 初版为「用户可选归档到 <code>artifacts/</code>」，<strong>2026-08-21 按用户决策收敛为「统一运行时目录 + 收尾告知位置」，不再弹归档决策</strong>）。<code>artifacts/</code> 目录保留（skill 内置 <code>skills/dev-comp/.gitignore</code> 忽略其内容、仅保留 <code>artifacts/README.md</code>），但<strong>仅作为历史归档的读取兜底</strong>，不再接收新产物；⚠️ 换仓库分发本 skill 时须连同该 <code>.gitignore</code> 一起复制。本小节固化六条原则，防止后续迭代被顺手改回。</p></blockquote><ol><li><strong>产物不入仓库</strong>：vue-amazing-ui 是开源项目，工作上下文/对齐清单/决策记录/度量是个人私有开发过程，进 git 污染公开历史，进 <code>.gitignore</code> 污染所有 fork 者（<strong>历史归档到 <code>ARTIFACTS_FALLBACK_DIR</code> 的快照同样不入库</strong>，见第 6 条）</li><li><strong>软复用产物沿用固有约定</strong>：devlog（<code>~/.codebuddy/dev-logs/{YYYYMMDD}_{类型}_{简述}/devlog.md</code>，tech-doc 实际规范，目录名由 tech-doc 生成规则决定，<strong>禁止自拟</strong> <code>&lt;项目&gt;/&lt;分支&gt;/</code> 结构）、knowledge（<code>~/.codebuddy/knowledge/vue-amazing-ui/</code>）路径由被软复用的 tech-doc / knowledge-loop skill 硬编码，改不得也不该改（零硬依赖原则）</li><li><strong>自建产物物理隔离</strong>：工作上下文、metrics 放 dev-comp 专属根目录 <code>~/.codebuddy/dev-comp/</code>（<code>working-context/</code> + <code>metrics/</code> 子目录）。不与 dev-flow 混放，避免被 dev-flow 的 lint 全量扫描 / dashboard 统计 / 度量闸门校验误伤</li><li><strong>命名前缀不变</strong>：<code>vaui-</code> 前缀保留，专属目录内按组件检索不受影响</li><li><strong>目录自举（写前必建）</strong>：<code>~/.codebuddy/dev-comp/</code> 专属目录首次使用不存在，阶段 0 初始化与阶段 5 写 metrics 前必须 <code>mkdir -p</code> 兜底，禁止假设目录已存在</li><li><strong>产物归档（统一运行时目录 · 2026-08-19 固化 · 2026-08-21 修订）</strong>：产物<strong>一律留在 <code>~/.codebuddy/</code> 运行时目录</strong>（原位即归档，<strong>不搬移、不产生副本</strong>）；阶段 5 收尾<strong>不弹归档决策</strong>，只需在 Gate 5 报告中<strong>告知四项产物位置</strong>（working-context / metrics / devlog / knowledge）。<code>ARTIFACTS_FALLBACK_DIR</code> 仅保留为<strong>历史归档的读取兜底</strong> —— 阶段 0 接续扫描按「<strong>运行时目录优先 → 兜底</strong>」两级顺序（详见 <code>references/flow.md</code> 阶段 0），命中历史归档时复制回运行时目录恢复活跃状态；该目录<strong>不再作为新产物的归档目标</strong></li></ol><h2 id="触发规则" tabindex="-1">触发规则 <a class="header-anchor" href="#触发规则" aria-label="Permalink to &quot;触发规则&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:left;">信号</th><th style="text-align:left;">行为</th></tr></thead><tbody><tr><td style="text-align:left;"><code>dc:</code>（技能名 <code>dev-comp</code> 也生效）</td><td style="text-align:left;">进入 dev-comp，提示指定组件名</td></tr><tr><td style="text-align:left;"><code>dc: {组件名}</code> / <code>dc: 开发 Menu</code></td><td style="text-align:left;">进入，开发指定组件</td></tr><tr><td style="text-align:left;"><code>dc:status</code> / <code>dc:st</code></td><td style="text-align:left;">查看当前组件进度</td></tr><tr><td style="text-align:left;">仅输入 <code>dc:</code>（无后续）</td><td style="text-align:left;">提示补充组件名</td></tr><tr><td style="text-align:left;"><code>组件开发</code> / <code>开发 {X} 组件</code> / <code>完善 {X} 组件</code> / <code>补全 {X} 组件</code></td><td style="text-align:left;">进入 dev-comp</td></tr><tr><td style="text-align:left;">仅提及组件但无开发意图（如&quot;看看 Menu 怎么实现的&quot;）</td><td style="text-align:left;">不触发，普通对话</td></tr></tbody></table><p><strong>优先级</strong>：incident-triage（告警硬触发）&gt; 用户显式 dev-flow 命令 &gt; dev-comp &gt; 普通对话。 <strong>与 dev-flow 互斥</strong>：用户显式用 <code>dev-flow</code> 命令时走 dev-flow；用 <code>dc:</code> / <code>dev-comp</code> 或组件开发意图时走本 skill。二者不同时激活。</p><h2 id="_6-阶段-gate-流程总览" tabindex="-1">6 阶段 + Gate 流程总览 <a class="header-anchor" href="#_6-阶段-gate-流程总览" aria-label="Permalink to &quot;6 阶段 + Gate 流程总览&quot;">​</a></h2><blockquote><p>术语约定：<strong>antd = Ant Design Vue</strong>（官网 <a href="https://www.antdv.com/" target="_blank" rel="noreferrer">https://www.antdv.com/</a> ），非 React 版 ant.design；本文档中 antdv 与 antd 同义。 每阶段完成后插入一道 🚦 <strong>Gate</strong>：先输出「阶段完成报告」，再弹 <code>ask_followup_question</code>（✅ 继续 / ⏸️ 暂停 / ⬅️ 回退），<strong>未获用户确认不得进入下一阶段</strong>。 「一次性走完」仅指<strong>不分 P1-Pn</strong>（单轮交付），Gate 仍逐阶段必弹，不得跳过。</p></blockquote><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>阶段 0  接续/初始化   → 读/建精简工作上下文 + todo plan + 分阶段决策</span></span>
<span class="line"><span>阶段 1  准备          → 分支 + 建目录/配置 + 注册占位（含 types/global-components.d.ts 登记）</span></span>
<span class="line"><span>阶段 2  组件本体      → 对照antdv/naive 源码开发（API 四维 + 渲染分支 + 先搜索复用项目资产）</span></span>
<span class="line"><span>阶段 3  演示用例      → 完整复制官网用例（顺序一致）+ 双组件对照（src/views/xxx/Index.vue + index.ts）（⚠️ 对照为验收期临时结构，阶段 5 收尾清除）</span></span>
<span class="line"><span>阶段 4  文档          → docs 复用演示页 + 周边文档联动</span></span>
<span class="line"><span>阶段 5  验收收尾      → 配置项终检 + 基线全量勾销 + lint+type-check+浏览器对照 → devlog+metrics+knowledge → **清除对照 + 交付前精修**（品牌清零/注释/Props 排序/用例布局/docs 对齐 + 复验）→ smart-commit → 引导发布（合入 main + 构建发布）</span></span></code></pre></div><p><strong>各阶段对应 Gate</strong>：Gate 0 确认组件名/分阶段计划/参考源/项目特有需求 · Gate 1 确认分支/目录/注册骨架（含全局类型声明登记）· Gate 2 确认功能 + API 四维/渲染分支/Demo 用例对齐清单 · Gate 3 确认演示页完整复制官网用例（顺序一致）+ 双组件对照 · Gate 4 确认文档完整 · Gate 5 配置项终检 + 基线全量勾销 + 交付前精修记录 + 确认验收结果 + 提交。</p><blockquote><p>Gate 报告模板 + 交互式选项定义 → <code>references/flow.md</code> §Gate 门控机制 完整执行规范 → <code>read_file(&quot;references/flow.md&quot;)</code></p></blockquote><h2 id="能力复用索引-软复用-零硬依赖-dev-flow" tabindex="-1">能力复用索引（软复用，零硬依赖 dev-flow） <a class="header-anchor" href="#能力复用索引-软复用-零硬依赖-dev-flow" aria-label="Permalink to &quot;能力复用索引（软复用，零硬依赖 dev-flow）&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:left;">能力</th><th style="text-align:left;">复用方式</th><th style="text-align:left;">何时用</th><th style="text-align:left;">详见</th></tr></thead><tbody><tr><td style="text-align:left;">工作上下文</td><td style="text-align:left;">自建精简版模板</td><td style="text-align:left;">阶段 0 建/接续</td><td style="text-align:left;"><code>templates/working-context-lite.tpl.md</code></td></tr><tr><td style="text-align:left;">plan</td><td style="text-align:left;"><code>todo_write</code> 工具</td><td style="text-align:left;">阶段 0 列计划</td><td style="text-align:left;">—</td></tr><tr><td style="text-align:left;">开发日志</td><td style="text-align:left;"><code>use_skill(&#39;tech-doc&#39;)</code></td><td style="text-align:left;">阶段 5 收尾</td><td style="text-align:left;"><code>references/capability-reuse.md</code></td></tr><tr><td style="text-align:left;">度量采集</td><td style="text-align:left;">自建精简 YAML</td><td style="text-align:left;">阶段 5 收尾</td><td style="text-align:left;"><code>templates/metrics-lite.tpl.yaml</code></td></tr><tr><td style="text-align:left;">知识沉淀</td><td style="text-align:left;"><code>use_skill(&#39;knowledge-loop&#39;)</code></td><td style="text-align:left;">阶段 2 检索 / 阶段 5 沉淀</td><td style="text-align:left;"><code>references/capability-reuse.md</code></td></tr><tr><td style="text-align:left;">提交</td><td style="text-align:left;"><code>use_skill(&#39;smart-commit&#39;)</code></td><td style="text-align:left;">阶段 5 提交</td><td style="text-align:left;"><code>references/capability-reuse.md</code></td></tr><tr><td style="text-align:left;">交互验收 e2e</td><td style="text-align:left;"><code>use_skill(&#39;e2e-testing&#39;)</code>（可选）</td><td style="text-align:left;">阶段 5 关键交互用例</td><td style="text-align:left;"><code>references/checklists.md</code> §交互操作清单</td></tr><tr><td style="text-align:left;">配置项终检 + 精修校验 + 提交红线校验</td><td style="text-align:left;"><code>scripts/validate-component.sh</code>（本 skill 自带）</td><td style="text-align:left;">阶段 5 收尾必跑（Gate 5 数据源）</td><td style="text-align:left;"><code>references/checklists.md</code> §发布前配置项终检</td></tr></tbody></table><blockquote><p>⚠️ 上述被调 skill 缺失时<strong>优雅降级</strong>：跳过该环节并一句话提示用户，不阻断主流程。</p></blockquote><h2 id="按需加载索引" tabindex="-1">按需加载索引 <a class="header-anchor" href="#按需加载索引" aria-label="Permalink to &quot;按需加载索引&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:left;">场景</th><th style="text-align:left;">加载文件</th></tr></thead><tbody><tr><td style="text-align:left;">执行任一阶段</td><td style="text-align:left;"><code>references/flow.md</code></td></tr><tr><td style="text-align:left;">项目规范权威源映射 / 项目结构/注册链路</td><td style="text-align:left;"><code>references/project-map.md</code></td></tr><tr><td style="text-align:left;">参考源路径 + antdv/naive 取舍 + <strong>渲染分支对齐</strong></td><td style="text-align:left;"><code>references/reference-sources.md</code></td></tr><tr><td style="text-align:left;">找可复用的项目已有资产</td><td style="text-align:left;"><code>references/reusable-assets.md</code></td></tr><tr><td style="text-align:left;">全链路 checklist（含 <strong>F 类组件规范</strong> + <strong>G 类交付前精修</strong>）+ 发布前配置项终检</td><td style="text-align:left;"><code>references/checklists.md</code></td></tr><tr><td style="text-align:left;"><strong>交付前精修与三方一致性</strong>（品牌清除 / 注释精修 / Props 排序 / 用例排序布局 / docs↔views↔源码对账）</td><td style="text-align:left;"><code>references/refine-spec.md</code></td></tr><tr><td style="text-align:left;">新增组件联动配置地图（⭐易遗漏点全集，含 ⑮ 全局类型声明）</td><td style="text-align:left;"><code>references/linkage-map.md</code></td></tr><tr><td style="text-align:left;">用例标题/简介描述规范（权威源 + 同步）</td><td style="text-align:left;"><code>references/demo-description.md</code></td></tr><tr><td style="text-align:left;">changelog 编写规范（版本号升级 + 双处同步 + 章节唯一性）</td><td style="text-align:left;"><code>references/changelog-spec.md</code></td></tr><tr><td style="text-align:left;">发布流程（合入 main + 构建发布 + 清理）</td><td style="text-align:left;"><code>references/release-flow.md</code></td></tr><tr><td style="text-align:left;">如何软复用 dev-flow 能力 + 降级</td><td style="text-align:left;"><code>references/capability-reuse.md</code></td></tr></tbody></table><h2 id="核心红线-继承项目规范" tabindex="-1">核心红线（继承项目规范） <a class="header-anchor" href="#核心红线-继承项目规范" aria-label="Permalink to &quot;核心红线（继承项目规范）&quot;">​</a></h2><ul><li>❌ 禁止自动 <code>git commit</code>：commit message仅生成，用户明确选择才提交（用 smart-commit）</li><li>❌ commit 格式：<code>&lt;type&gt;: &lt;description&gt;</code>（<strong>无 scope</strong>，项目 commitlint scope-empty）</li><li>❌ <strong>提交前 git 身份实测</strong>：<code>git config user.name/email</code> 实测值必须与工作上下文 <code>git_identity</code> 一致，不符拦截、用户决策后继续；提交后 <code>git log -1 --format=&#39;%h&#39;</code> 实测 hash 回填（详见 <code>references/flow.md</code> 阶段 5 第 4 步）</li><li>❌ 先搜索后编码：新增能力前先查项目已有资产（<code>references/reusable-assets.md</code>）</li><li>✅ 实现方式复用优先级：项目已有组件/功能/样式/布局/逻辑 &gt; antdv 源码实现 &gt; naive 源码实现 &gt; 自研；<strong>组件库已有的功能/样式/布局/逻辑优先复用，禁止重新开发</strong>（详见 <code>references/reference-sources.md</code>）</li><li>✅ 主题 light/dark 双份；链式访问用可选链 <code>?.</code>；禁 any；SSR 安全（禁裸用 window/document）</li><li>✅ 大组件分阶段交付（P1-Pn），避免半成品</li><li>✅ 验收标准：演示页与antdv/naive 真身并排 1:1 对照（本项目组件在左/上，官网组件在右/下）+ 用例顺序符合已确认排序锚点（默认官网序，见 <code>refine-spec.md</code> §4.1）+ 浏览器实测（⚠️ 对照仅为验收手段：阶段 3 引入 → 阶段 5 浏览器实测验收 → <strong>验收完成后由阶段 5 第 5 步清除</strong>，演示页回归纯本库组件）</li><li>✅ 清单即验收基线：阶段 2 对齐清单（API 四维 + <strong>渲染分支</strong> + Demo 用例）+ naive 差异登记 + 阶段 0 项目特有需求 = 阶段 5 验收唯一对账标准，Gate 5 全量回显勾销，❌ 项必带处置码（<code>延后 P{n}</code> / <code>不覆盖（理由）</code> / <code>待用户确认</code>），禁止摘要式报告</li><li>✅ <strong>联动清单即注册基线</strong>：<code>references/linkage-map.md</code> 是「新增组件全量联动点」唯一权威源（含 ⭐ 易遗漏点：<strong><code>style-deps.ts</code> 四张表</strong>（D 方案单一数据源，含复合组件逐子组件登记）/ 组件总数 4 处 / components.d.ts 幽灵声明 / App.vue 孤儿变量 / <strong>⑮ types/global-components.d.ts 全局类型声明登记</strong>）。阶段 1/4/5 逐项勾销，<strong>禁止靠记忆「顺手补几处」</strong>；确定性 grep 自检已收拢于 <code>scripts/validate-component.sh</code>（Gate 5 必跑），宣告完成前必须实测</li><li>✅ <strong>配置项终检即发布基线</strong>：<code>references/checklists.md</code> §发布前配置项终检 是阶段 5 验收时固定配置项（代码注册/文档联动/残留清理/一致性/<strong>组件规范</strong>/<strong>交付前精修</strong>）的唯一权威源，Gate 5 必须全量逐项回显勾销 + grep 自检实测；<strong>埋入阶段（1/4）的检查不能替代终检</strong>，发布前必须全量回检</li><li>✅ <strong>品牌信息 0 残留红线</strong>（脚本 C5 拦截）：交付物不得残留 antdv / naive 的品牌内容与信息——库名（<code>antdv</code>/<code>antd</code>/<code>ant-design-vue</code>/<code>Ant Design Vue</code>/<code>naive</code>/<code>naive-ui</code>）、<code>&lt;a-xxx&gt;</code> 真身标签、<code>avalue*</code>·<code>aoptions*</code>·<code>antdTheme</code> 等残留，<strong>以及注释里的来源标注</strong>；<strong>范围含 docs 全部内容（组件文档 + changelog/features/index）</strong>、组件源码、演示页、单测、根级 README。<strong>两段口径</strong>（⚠️ 兼容分批开发/分批提交）=「①本分支<strong>净改动</strong>新增行（<code>git diff &lt;base&gt;</code> 基点→当前工作区，一次覆盖已提交的每一批 + 未提交改动）②未跟踪新增文件全文」（<strong>存量不追溯</strong>）——只查工作区会漏掉已提交的批次，base 取 <code>--base</code> / 工作上下文 <code>base_ref</code> / <code>merge-base 主干</code>（指定或登记的 ref <strong>无法解析</strong>时脚本给「C5 判定不完整」WARN 并回显该 ref——此 WARN ≠ 通过，须核对 ref 后重跑）；净改动为 0 但已提交批次仍有残留时会 WARN「删除须随本次 commit 提交，否则复活」。<strong>处置</strong>：品牌对比/差异说明类内容（docs「与 XX 的差异」段落、changelog「对齐 XX」说明）<strong>整段直接删除、不保留</strong>。<strong>例外</strong>：<code>@ant-design/*</code> 基础包（图标/色板）不清除。详见 <code>references/refine-spec.md</code> §1</li><li>✅ <strong>交付前精修必做</strong>（阶段 5 第 5 步，红线）：<strong>范围 = 四来源</strong>（<code>refine-spec.md</code> §0.1）——组件源码 / 演示页 / docs / <strong>git 已提交批次</strong>；<strong>起手先取范围清单</strong>：<code>git diff --name-only &lt;base&gt;</code> + <code>git ls-files --others --exclude-standard</code>（脚本 <strong><code>G0</code></strong> 已按三载体分类输出），一次覆盖「已提交的每一批 + 未提交改动」；❌ <strong>只看工作区 <code>git diff</code></strong> 在分批提交场景会整段漏检已提交批次，❌ <strong>范围与本轮改动面无关</strong>（本轮没改的载体也要逐项过）。每次组件开发收尾都要①<strong>组件源码注释精修</strong>（三层结构 / 删复述与过期注释 / 删品牌来源标注）②<strong>Props 排序</strong>（六段式分组 → 双向绑定→内容数据→形态外观→状态反馈→行为交互→进阶透传，段内语义相邻；增量属性<strong>插入所属段</strong>、❌ 禁追末尾；源码顺序 ≡ docs <code>## APIs</code> 表顺序）③<strong>演示用例排序与布局</strong>（<strong>排序锚点先确认</strong>，见 <code>refine-spec.md</code> §4.1：A 官网保序＝默认，官网用例保序 / 新增插回原序 / 特有用例归末尾；B 合理性优先＝用户明确「无需对齐官网」时启用，主题聚簇 + 由浅入深 / 特有用例就近插入；单例整块 / 多例并排 / 交互类全宽，同类型布局一致）④<strong>docs ↔ views 用例对齐</strong>（数量/顺序/标题逐字）+ <strong>三方一致性对照</strong>（源码↔docs↔views 全维度）。⚠️ 结论为「无需改动」也必须附<strong>逐载体核对证据</strong>（逐层 / 逐区结论），<code>grep</code> 抽检 ≠ 精修。精修属实质改动 → <strong>必须复验</strong>（lint + type-check + 浏览器实测）。权威源 <code>references/refine-spec.md</code>，脚本 <code>G0</code>/<code>C5</code>/<code>G2</code>/<code>G4</code> 拦截</li><li>✅ <strong>验收完成 ≠ 任务结束</strong>：验收通过后按 <code>references/release-flow.md</code> 引导「合入 main（GitHub PR）→ main 上构建发布（<code>pnpm pub</code>，执行前用户逐条确认）→ 发布后清理（删 feat 分支）」；❌ 严禁在 feat 分支上执行发布；用户本轮不发布则写入工作上下文接续指引</li><li>✅ <strong>组件规范红线</strong>（权威源 <code>development/component-design.md</code>，脚本 F1-F3 拦截）：插槽类型 <code>export interface {组件名}Slots</code> + <code>defineSlots&lt;{组件名}Slots&gt;()</code>（⚠️ <strong>无插槽组件豁免</strong>——内部渲染内核等无 <code>&lt;slot</code>/<code>useSlotsExist</code>/<code>$slots</code> 的 .vue 不强制声明；复合组件逐 .vue 校验）；根类名 = <code>{组件名}-wrap</code>（❌ 禁 <code>m-</code> / <code>vui-</code> 等自拟前缀）；Props/Slots 注释禁写 <code>string | slot</code>；<code>useSlotsExist</code> 只传实际使用的插槽名</li><li>✅ <strong>全局类型声明必登记</strong>（脚本 F5 拦截）：新增组件 / 复合子组件 / Provider 必须在 <code>types/global-components.d.ts</code> 登记（<code>linkage-map.md</code> §⑮）——⚠️ 漏登记<strong>不报错、<code>pnpm type-check</code> 仍 PASS</strong>，属静默失效，只能靠脚本兜底</li><li>✅ <strong>对齐维度 = API 四维 + 渲染分支 + Demo 用例</strong>（三者缺一不可）：必须读参考库渲染实现<strong>逐分支</strong>核对（字符串/VNode/空值兜底/双源优先级/数组归一/falsy 边界），差异显式决策并登记（详见 <code>reference-sources.md</code> §渲染分支与兜底逻辑对齐）</li><li>✅ <strong>演示页红线</strong>（脚本 F7/F8 拦截）：分区按已确认排序锚点<strong>排列</strong>（默认官网原序，锚点定义见 <code>refine-spec.md</code> §4.1），但标题与 script 注释<strong>不带数字序号</strong>；本项目组件<strong>无需 import</strong>（全局注册，直接写 <code>&lt;Xxx&gt;</code> 标签）</li><li>✅ <strong>changelog 红线</strong>（脚本 F6 拦截）：版本章节<strong>唯一</strong>且严格递减；条目链接用<strong>站内相对路径 + 实测目录名（kebab）</strong>；<code>## future</code> 清单同步删除已落地组件；<code>package.json</code> 与 changelog 双处一致（<code>changelog-spec.md</code> §3.4）</li><li>✅ <strong>文档红线</strong>（脚本 B5/B6/B7/B8 拦截）：<code>## APIs</code> 必有且含 <code>### {组件名}</code> 子标题；<code>## Events</code> / <code>## Methods</code> 按组件实际能力（有 emit / <code>defineExpose</code> 才写）；类型列写真实 TS 类型（❌ 禁 <code>string | slot</code>）；<code>## Slots</code> 表列头「名称 | 说明 | 用法」+ 用法列 <code>v-slot:xxx</code></li></ul>`,31)])])}const m=e(s,[["render",n]]);export{p as __pageData,m as default};

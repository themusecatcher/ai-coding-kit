import{_ as t,o as d,c as o,a2 as c}from"./chunks/framework.BUwdTBzS.js";const m=JSON.parse('{"title":"dev-flow 触发与命令","description":"","frontmatter":{"title":"dev-flow 触发与命令"},"headers":[],"relativePath":"dev-flow/skill.md","filePath":"dev-flow/skill.md","lastUpdated":null}'),s={name:"dev-flow/skill.md"};function r(l,e,i,a,n,h){return d(),o("div",null,[...e[0]||(e[0]=[c(`<blockquote><p>📄 本页由源文件 <code>skills/dev-flow/SKILL.md</code> 自动投影生成（单一权威源）。请勿直接编辑本页。</p></blockquote><h1 id="dev-flow-触发与命令" tabindex="-1">dev-flow 触发与命令 <a class="header-anchor" href="#dev-flow-触发与命令" aria-label="Permalink to &quot;dev-flow 触发与命令&quot;">​</a></h1><blockquote><p><strong>分层加载架构</strong>：本文件为精简基线（~130 行），只保留 AI 做触发判断和流程入口所需的最小规则集。 详细规范按需加载：遇到复杂场景时 <code>read_file(&quot;references/skill-full.md&quot;)</code> 获取完整版本。</p></blockquote><h2 id="触发规则-速查" tabindex="-1">触发规则（速查） <a class="header-anchor" href="#触发规则-速查" aria-label="Permalink to &quot;触发规则（速查）&quot;">​</a></h2><blockquote><p>完整触发规则 → <code>.compiled/triggers.md</code>（按需加载）/ <code>references/skill-full.md</code>「触发规则」章节</p><p><strong>2026-06-01 改版</strong>：dev-flow 仅支持<strong>显式命令</strong>触发 + <strong>活跃流程相关时恢复</strong>，不再基于关键词主观判断自动触发。</p></blockquote><h3 id="触发判定-普通对话-→-dev-flow" tabindex="-1">触发判定（普通对话 → dev-flow） <a class="header-anchor" href="#触发判定-普通对话-→-dev-flow" aria-label="Permalink to &quot;触发判定（普通对话 → dev-flow）&quot;">​</a></h3><table tabindex="0"><thead><tr><th>信号类型</th><th>关键词/条件</th><th>行为</th></tr></thead><tbody><tr><td>显式命令</td><td><code>dev-flow</code> / <code>dev:</code> / <code>/dev-flow</code></td><td>进入统一流程</td></tr><tr><td>流程内同步</td><td><code>dev:sync</code> / <code>dev:s2</code></td><td>全量文档同步，支持任意时机调用（按实际文档存在性自动适配；详见 <code>references/in-flow-sync.md</code>）</td></tr><tr><td>子命令</td><td><code>dev:status</code> / <code>dev:st</code> / <code>dev:kb</code> / <code>dev:k</code> / <code>dev:metrics</code> / <code>dev:m</code> / <code>dev:onboard</code> / <code>dev:ob</code> / <code>dev:flowchart</code> / <code>dev:chart</code></td><td>对应子命令</td></tr><tr><td>帮助命令</td><td><code>dev:help</code> / <code>dev:h</code> / <code>--help</code> / <code>-h</code></td><td>加载 <code>references/help.md</code>，输出 <code>&lt;!-- OUTPUT_END --&gt;</code> 以上的全部内容原文（禁止改写），不进入步骤流程</td></tr><tr><td>帮助一致性检查</td><td><code>dev:help --check</code></td><td>运行 <code>scripts/lints/help-consistency.sh</code>，对比 help.md 与权威源的一致性，不进入步骤流程</td></tr><tr><td>菜单命令</td><td><code>dev:ask</code> / <code>dev:guide</code></td><td>加载 <code>references/menu.md</code>，弹出交互式功能菜单。不进入步骤流程，不输出步骤完成标记 JSON</td></tr><tr><td>修饰命令</td><td><code>--fast</code> / <code>--micro</code>（与基础命令组合）</td><td>修饰层叠加</td></tr><tr><td>活跃流程恢复</td><td><code>.active-flows/*.flow</code> 存在 + 用户消息与 <code>match_keywords</code>/<code>brief</code> 相关</td><td>v3 智能恢复网关（详见 <code>flow.md</code> §「多活跃流程处理」）</td></tr><tr><td>开发意图（<strong>不自动触发</strong>）</td><td><code>修复</code> / <code>优化</code> / <code>新需求</code> / <code>开发 xxx</code> / 任务平台 / Figma 等</td><td><strong>AI 在回复中主动建议</strong> <code>dev-flow</code> 命令，等待用户决策；不自动调用 <code>use_skill(&#39;dev-flow&#39;)</code></td></tr><tr><td>文档同步关键词</td><td><code>同步文档</code> / <code>全量同步</code> / <code>检查文档</code> / <code>更新文档</code></td><td>触发 <code>dev:sync</code>（.flow 存在→流程内模式，否则→独立模式匹配工作上下文；详见 <code>references/in-flow-sync.md</code>）</td></tr></tbody></table><p><strong>优先级</strong>：用户显式命令 &gt; 活跃流程恢复（相关时） &gt; 普通对话（含开发意图建议）</p><p><strong>强执行规则</strong>：</p><ul><li>一旦显式命令触发 dev-flow，<strong>严格执行完整流程</strong>，不得以&quot;任务简单&quot;为由跳步</li><li>AI <strong>严禁</strong>基于关键词主观判断自动触发；命中开发意图关键词时，必须在回复中提示 <code>dev-flow</code> 命令并等待用户决策</li><li>用户未用命令但要求改代码（如「直接改」） → 不进入 dev-flow，但仍须遵守 <code>开发规范-红线.mdc</code> 的 12 条红线和简化质量检查（详见 <code>~/.codebuddy/skills/dev-flow/references/no-dev-flow-mode.md</code>）</li></ul><h3 id="流程内信号-仅-dev-flow-已激活时生效-不属触发规则" tabindex="-1">流程内信号（仅 dev-flow 已激活时生效，不属触发规则） <a class="header-anchor" href="#流程内信号-仅-dev-flow-已激活时生效-不属触发规则" aria-label="Permalink to &quot;流程内信号（仅 dev-flow 已激活时生效，不属触发规则）&quot;">​</a></h3><blockquote><p>以下信号<strong>仅在 dev-flow 已激活的流程内</strong>生效，普通对话中说这些词<strong>不会触发任何行为</strong>。</p></blockquote><table tabindex="0"><thead><tr><th>流程内信号</th><th>关键词</th><th>作用</th></tr></thead><tbody><tr><td>迭代修复</td><td><code>提测反馈</code> / <code>继续上次需求</code> / <code>后端接口好了</code></td><td>已有 <code>.flow</code> 上下文中切到 <code>iteration-fix</code></td></tr><tr><td>批次继续</td><td><code>继续下一批</code> / <code>batch 2</code></td><td><code>batch_mode=true</code> 上下文中切批次</td></tr><tr><td>跨项目衔接</td><td><code>跨项目修复衔接</code> / <code>跨项目验证衔接</code></td><td>流程内加载 <code>cross-project-flow.md</code></td></tr><tr><td>修饰层切换</td><td><code>少问我</code> / <code>你决定就好</code> / <code>每步都问我</code></td><td>已激活流程中调整 <code>interaction_mode</code></td></tr><tr><td>需求漂移</td><td><code>产品说</code> / <code>刚和 XX 对齐</code> / <code>需求变了</code> / <code>其实是</code></td><td>已激活流程中加载 <code>references/drift-handling.md</code>（micro-fix 模式豁免）</td></tr></tbody></table><h2 id="命令速查" tabindex="-1">命令速查 <a class="header-anchor" href="#命令速查" aria-label="Permalink to &quot;命令速查&quot;">​</a></h2><blockquote><p>完整命令说明 → <code>.compiled/commands.md</code>（按需加载）</p></blockquote><table tabindex="0"><thead><tr><th>命令</th><th>快捷</th><th>说明</th></tr></thead><tbody><tr><td><code>dev:sync</code></td><td><code>dev:s2</code></td><td>全量文档同步，支持任意时机调用（按实际文档存在性自动适配）</td></tr><tr><td><code>--fast</code></td><td>—</td><td>精简交互模式（可与任意命令组合）</td></tr><tr><td><code>--micro</code></td><td>—</td><td>显式启动 micro-fix 模式（单文件 + ≤10 行 + 已知位置的快速修复）</td></tr><tr><td><code>dev:fix --drift</code></td><td>—</td><td>显式触发需求漂移（完成后自动调用 dev:sync 同步下游文档 + 门控校验）</td></tr><tr><td><code>dev:fix --iteration</code></td><td>—</td><td>显式触发迭代修复（调 iteration-fix-classify.sh --explicit-trigger iteration）</td></tr><tr><td><code>dev:kb</code></td><td><code>dev:k</code></td><td>知识库管理</td></tr><tr><td><code>dev:status</code></td><td><code>dev:st</code></td><td>工作上下文进度概览</td></tr><tr><td><code>dev:status --trace</code></td><td>—</td><td>实时观测（Token/红牌/步骤耗时）</td></tr><tr><td><code>dev:metrics</code></td><td><code>dev:m</code></td><td>度量查看（支持 --all/--trend/--dashboard）</td></tr><tr><td><code>dev:onboard</code></td><td><code>dev:ob</code></td><td>知识库平台 profile 生成/刷新（按需，用户主动触发）</td></tr><tr><td><code>dev:flowchart</code></td><td><code>dev:chart</code></td><td>生成/更新 dev-flow 流程图（md + html + png）</td></tr><tr><td><code>dev:ask</code></td><td><code>dev:guide</code></td><td>交互式功能菜单（覆盖所有 dev-flow 功能）</td></tr><tr><td><code>dev:help</code></td><td><code>dev:h</code></td><td>显示帮助信息（命令/模式/工作流）；<code>--check</code> 运行一致性检查</td></tr></tbody></table><h2 id="必要节点-ai-主动弹框提醒-dev-sync-触发表" tabindex="-1">必要节点 AI 主动弹框提醒（dev:sync 触发表） <a class="header-anchor" href="#必要节点-ai-主动弹框提醒-dev-sync-触发表" aria-label="Permalink to &quot;必要节点 AI 主动弹框提醒（dev:sync 触发表）&quot;">​</a></h2><blockquote><p>权威表格。AI 在 dev-flow 流程内的以下节点必须主动调用 <code>ask_followup_question</code> 弹出 dev:sync 提醒。 详细规则 → <code>references/in-flow-sync.md</code> §1.2。 dev:sync ≠ 5.5b 替代品的单一权威源 → <code>~/.codebuddy/rules/AI行为规范.mdc</code> §「主动文档同步弹框提醒」</p></blockquote><table tabindex="0"><thead><tr><th>#</th><th>触发节点</th><th>触发条件</th><th style="text-align:center;">必弹</th><th>文档引用</th></tr></thead><tbody><tr><td>1</td><td>5.5 静默累计 ≥3 次</td><td><code>.flow.silent_55_count ≥ 3</code>（post-step.sh 物理维护）</td><td style="text-align:center;">✅</td><td><code>references/iteration-fix.md</code> §四 / <code>steps/step-5-execute.md</code> §2.5</td></tr><tr><td>2</td><td>完整模式步骤 7 完成后追加改动</td><td><code>caller=full-7</code> 完成 + 步骤 8/9/<strong>10</strong> 入口 git diff vs <code>last_sync_diff_sha</code> 非空</td><td style="text-align:center;">✅</td><td><code>steps/step-8-10-full.md</code> 入口钩子</td></tr><tr><td>3</td><td>drift-handling §步骤 3.5 完成后</td><td>CR 登记后</td><td style="text-align:center;">🟡 建议（不阻断）</td><td><code>references/drift-handling.md</code> §步骤 3.5</td></tr><tr><td>4</td><td>跨会话恢复时检测到 <code>.flow.status=paused_for_sync</code></td><td>上次 sync 未完成</td><td style="text-align:center;">✅</td><td><code>references/in-flow-sync.md</code> §五</td></tr></tbody></table><p><strong><code>sync_reminder_disabled: true</code> 时的处理</strong>：仅对 #1 / #2 静默跳过；#4（恢复未完成的 sync）仍必弹（已经是用户主动召唤过的，需要给出收尾交互）；#3 本就是建议级，无影响。</p><h2 id="流程加载指令-prompt-chaining-架构" tabindex="-1">流程加载指令（Prompt Chaining 架构） <a class="header-anchor" href="#流程加载指令-prompt-chaining-架构" aria-label="Permalink to &quot;流程加载指令（Prompt Chaining 架构）&quot;">​</a></h2><table tabindex="0"><thead><tr><th>流程</th><th>加载文件</th></tr></thead><tbody><tr><td>开发流程</td><td><code>read_file(&quot;flow.md&quot;)</code> → <code>read_file(&quot;steps/step-router.md&quot;)</code> → 按需加载各步骤</td></tr></tbody></table><p><strong>Prompt Chaining 硬性规则</strong>：</p><ul><li>❌ 禁止一次性加载多个步骤文件</li><li>❌ 禁止未加载步骤文件就执行该步骤</li><li>❌ 禁止跳过结构化完成标记 JSON 的输出</li></ul><h2 id="工作上下文持久化-核心要点" tabindex="-1">工作上下文持久化（核心要点） <a class="header-anchor" href="#工作上下文持久化-核心要点" aria-label="Permalink to &quot;工作上下文持久化（核心要点）&quot;">​</a></h2><blockquote><p>完整规则、命名规则、项目缩写映射表 → <code>references/working-context.md</code>（规则单一权威源） 完整文件模板（YAML + Markdown 区块） → <code>references/templates/working-context.tpl.md</code>（创建新文件前必须加载）</p></blockquote><ul><li><strong>创建前</strong>：① <code>read_file(&quot;references/templates/working-context.tpl.md&quot;)</code> 加载模板 → ② <code>read_file(&quot;references/working-context.md&quot;)</code> 查规则（命名/项目缩写/校验）→ ③ <strong>跨项目复用预检</strong>（见下方小节，显式命令入口必经，堵&quot;恢复网关不生效&quot;漏洞）→ ④ <code>ls</code> 扫描目录匹配已有文件 → ⑤ 匹配到则更新，未匹配到才创建</li><li><strong>创建/重命名后</strong>：必须调用 <code>bash scripts/validate-working-context.sh &lt;文件路径&gt;</code>，返回非 0 → 禁止继续</li><li>❌ 禁止不扫描就直接创建 / 禁止同一需求创建多个文件 / 禁止未加载模板就凭记忆创建</li></ul><h3 id="🆕-跨项目复用预检-显式命令入口堵漏-2026-07-02" tabindex="-1">🆕 跨项目复用预检（显式命令入口堵漏，2026-07-02） <a class="header-anchor" href="#🆕-跨项目复用预检-显式命令入口堵漏-2026-07-02" aria-label="Permalink to &quot;🆕 跨项目复用预检（显式命令入口堵漏，2026-07-02）&quot;">​</a></h3><blockquote><p><strong>设计动机</strong>：v3 智能恢复网关（<code>references/active-flows.md</code> §「自动恢复决策逻辑」）<strong>只对非显式命令生效</strong>；用户在 B 项目直接输入 <code>dev-flow</code> 会绕过网关，导致 A 项目留下的 <code>cross_project.status=pending_fix</code> 工作上下文被遗忘、B 项目另开新文件。本预检<strong>仅</strong>在阶段 0 创建工作上下文前执行一次。</p></blockquote><p><strong>执行 5 步</strong>（AI 必走，约 1 次 <code>ls</code> + 1 次 <code>head</code> + 0~1 次 <code>ask_followup_question</code>）：</p><ol><li><code>ls ~/.codebuddy/working-context/.active-flows/*.flow 2&gt;/dev/null</code> 拿活跃锁；空 → 跳到步骤 4 普通匹配</li><li>对每个 .flow，对应工作上下文 .md 取 YAML 头（<code>head -n 40 &lt;name&gt;.md</code> 找 <code>cross_project</code> 块）</li><li>命中条件：<code>cross_project.enabled=true &amp;&amp; status=pending_fix</code> 且当前 workspace 与 <code>fix_workspace</code> 一致（或 <code>fix_workspace</code> 为空时弹选项让用户确认）</li><li>命中 → <code>ask_followup_question</code> 弹「🔁 复用工作上下文 / 🆕 新建 / 👀 查看详情」三选项；用户选复用 → 跳到已有上下文路径，<strong>不创建</strong>新文件，按 <code>references/cross-project/handoff.md</code> §「衔接后行为」执行阶段 0 增量理解（步骤 1 跳过）</li><li>未命中 / 用户选新建 → 继续原流程的步骤 ④ 普通匹配</li></ol><p><strong>反模式</strong>：❌ 跳过本预检直接创建（绕开本规约即视为违反强执行规则） / ❌ 预检中无 <code>cross_project.enabled=true</code> 时弹选项（无意义打扰）。</p><h2 id="步骤切换门控-硬性规则" tabindex="-1">步骤切换门控（硬性规则） <a class="header-anchor" href="#步骤切换门控-硬性规则" aria-label="Permalink to &quot;步骤切换门控（硬性规则）&quot;">​</a></h2><blockquote><p>完整门控规范 → <code>references/gate-validator.md</code><strong>🆕 程序化执行入口</strong> → <code>scripts/hooks/</code>（PreStepLoad / PostStepComplete 钩子）+ <code>config/gates.yaml</code> + <code>config/hooks.json</code></p></blockquote><p>每步骤完成后必须：① 输出结构化完成标记 JSON → ② 更新工作上下文 → ③ 调用 validate-output.sh 机器校验（带 flow-name 参数）→ ④ 门控验证通过（<code>.validated</code> 已生成）→ ⑤ 才能加载下一步骤</p><p><strong>🆕 推荐：使用统一钩子入口（替代各类零散调用）</strong>：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 步骤完成后（替代直接调 validate-output.sh）：自动跑 validate + doc-platform-lint</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bash</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ~/.codebuddy/skills/dev-flow/scripts/hooks/post-step.sh</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">step-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">json-fil</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">e</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">flow-nam</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">e</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 加载下一步骤前（替代手动 ls 检查）：自动跑物理检查点白名单 + 步骤 5 加上编码前置硬卡点</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bash</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ~/.codebuddy/skills/dev-flow/scripts/hooks/pre-step.sh</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">flow-nam</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">e</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">target-step-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span></code></pre></div><p><strong>Schema 机器校验</strong>（兼容旧路径，仍可直接调用）：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>bash ~/.codebuddy/skills/dev-flow/scripts/validate-output.sh &lt;step-id&gt; &lt;json-file&gt; &lt;flow-name&gt;</span></span></code></pre></div><ul><li><strong>必须</strong>传 <code>&lt;flow-name&gt;</code>（从工作上下文文件名去 .md 推导），脚本才会自动 touch <code>.step-{N}.validated</code> 物理检查点</li><li>返回码 0 → 加载下一步骤；≠ 0 → 补齐后重试（详见红牌 #14）</li><li>Schema 文件：<code>references/schemas/all-steps.schema.json</code></li></ul><h2 id="核心原则-精要" tabindex="-1">核心原则（精要） <a class="header-anchor" href="#核心原则-精要" aria-label="Permalink to &quot;核心原则（精要）&quot;">​</a></h2><blockquote><p>§1~§12 由 alwaysApply 规则自动加载。§13~§18 为 dev-flow 扩展层。 完整说明 → <code>.compiled/principles.md</code> / <code>references/core-principles.md</code></p></blockquote><p><strong>扩展层（§13~§18）</strong>：不确定就先确认 | 向用户解释 | 深度思考 | 不可变性优先 | 文件≤500行/函数≤80行 | 风格一致性</p><h2 id="模式矩阵-5-种基础模式-1-种修饰层" tabindex="-1">模式矩阵（5 种基础模式 + 1 种修饰层） <a class="header-anchor" href="#模式矩阵-5-种基础模式-1-种修饰层" aria-label="Permalink to &quot;模式矩阵（5 种基础模式 + 1 种修饰层）&quot;">​</a></h2><blockquote><p>完整矩阵 → <code>references/mode-matrix.md</code></p></blockquote><ul><li><strong>基础模式</strong>（mode，互斥单选）：<code>standard</code> / <code>full</code> / <code>iteration-fix</code> / <code>batch</code> / <code>cross-project</code> + <code>micro-fix</code>（轻量快速修复专项模式）</li><li><strong>修饰层</strong>（interaction_mode，正交叠加）：<code>streamlined</code>（启用后仅调节交互频率，不改变基础模式）</li></ul><p>遇到模式冲突或不确定时 → <code>read_file(&quot;references/mode-matrix.md&quot;)</code></p><h2 id="按需加载索引" tabindex="-1">按需加载索引 <a class="header-anchor" href="#按需加载索引" aria-label="Permalink to &quot;按需加载索引&quot;">​</a></h2><blockquote><p>遇到对应场景时加载，不提前加载</p></blockquote><table tabindex="0"><thead><tr><th>场景</th><th>加载文件</th></tr></thead><tbody><tr><td>触发规则边界情况</td><td><code>.compiled/triggers.md</code></td></tr><tr><td>交互式功能菜单</td><td><code>references/menu.md</code></td></tr><tr><td>帮助/命令查询</td><td><code>references/help.md</code></td></tr><tr><td>帮助一致性检查</td><td><code>scripts/lints/help-consistency.sh</code></td></tr><tr><td>命令详细说明</td><td><code>.compiled/commands.md</code></td></tr><tr><td>核心原则详细说明</td><td><code>.compiled/principles.md</code> / <code>references/core-principles.md</code></td></tr><tr><td>模式切换/冲突</td><td><code>references/mode-matrix.md</code></td></tr><tr><td>工作上下文创建</td><td><code>references/working-context.md</code></td></tr><tr><td>迭代修复</td><td><code>references/iteration-fix.md</code></td></tr><tr><td>跨项目联调</td><td><code>references/cross-project-flow.md</code></td></tr><tr><td>Figma 设计稿</td><td><code>references/figma-flow.md</code></td></tr><tr><td>技术方案文档</td><td><code>references/tech-proposal-flow.md</code></td></tr><tr><td>精简交互模式</td><td><code>references/interaction-mode.md</code></td></tr><tr><td>Token 紧张</td><td><code>references/token-management.md</code></td></tr><tr><td>对话质量守卫（长对话/预警）</td><td><code>references/conversation-quality.md</code></td></tr><tr><td>需求漂移（沟通回流/方案否定/澄清调整）</td><td><code>references/doc-sync-rules.md</code> → <code>references/drift-handling.md</code></td></tr><tr><td>并行执行策略</td><td><code>references/shared-rules.md</code> §3</td></tr><tr><td>Git 分支规范</td><td><code>references/shared-rules.md</code> §6</td></tr><tr><td>Commit 生成</td><td><code>references/shared-rules.md</code> §1 → <code>use_skill(&#39;smart-commit&#39;)</code></td></tr><tr><td>知识库管理</td><td><code>use_skill(&#39;knowledge-loop&#39;)</code></td></tr><tr><td>浏览器工具选型/UI 调试/性能分析</td><td><code>use_skill(&#39;browser-toolkit&#39;)</code></td></tr><tr><td>知识库平台 外援（默认沉默，仅在信号命中时触发）</td><td><code>references/remote-knowledge.md</code>（单一权威源：信号定义 + 项目映射 + Token 策略 + 噪声过滤）/ <code>references/onboard-flow.md</code>（profile 生命周期）</td></tr><tr><td>完整 SKILL 规范</td><td><code>references/skill-full.md</code></td></tr><tr><td>🆕 程序化校验脚本（lints）</td><td><code>scripts/lints/{path,interactive-options,devlog-dir-name,doc-platform}-lint.sh</code></td></tr><tr><td>🆕 程序化前置校验（precheck）</td><td><code>scripts/precheck/{physical-checkpoint,step5-precheck}.sh</code></td></tr><tr><td>🆕 步骤前后统一钩子</td><td><code>scripts/hooks/{pre-step,post-step}.sh</code></td></tr><tr><td>🆕 状态机查询</td><td><code>scripts/state-machine.sh --query-next/--query-step7-variant/--list-steps</code></td></tr><tr><td>🆕 门控规则单一权威源</td><td><code>config/gates.yaml</code></td></tr><tr><td>🆕 Hook 注册表</td><td><code>config/hooks.json</code></td></tr></tbody></table><h2 id="help-md-一致性维护规则" tabindex="-1">help.md 一致性维护规则 <a class="header-anchor" href="#help-md-一致性维护规则" aria-label="Permalink to &quot;help.md 一致性维护规则&quot;">​</a></h2><blockquote><p>权威源文件（SKILL.md / mode-matrix.md / step-router.md）与 <code>references/help.md</code> 之间存在手工维护的信息冗余。修改权威源后 help.md 不会自动更新，需主动检查。</p></blockquote><h3 id="被动提醒-ai-自动执行" tabindex="-1">被动提醒（AI 自动执行） <a class="header-anchor" href="#被动提醒-ai-自动执行" aria-label="Permalink to &quot;被动提醒（AI 自动执行）&quot;">​</a></h3><p>当对话中<strong>非 dev-flow 流程内</strong>修改了以下文件时，AI 必须在回复末尾主动提醒：</p><ul><li><code>SKILL.md</code></li><li><code>references/mode-matrix.md</code></li><li><code>steps/step-router.md</code></li></ul><p>提醒格式：<code>💡 提醒：help.md 可能与权威源存在不一致，建议运行 \\</code>dev:help --check\` 检查。\`</p><ul><li>❌ 禁止在 dev-flow 流程中触发（与开发无关）</li><li>❌ 禁止自动执行检查或自动修改 help.md</li><li>✅ 仅在修改权威源文件后提醒一次</li></ul><h3 id="已知结构性差异-非错误" tabindex="-1">已知结构性差异（非错误） <a class="header-anchor" href="#已知结构性差异-非错误" aria-label="Permalink to &quot;已知结构性差异（非错误）&quot;">​</a></h3><p>以下差异是 help.md 与权威源之间的设计差异，<code>help-consistency.sh</code> 会报告但<strong>不需要修正</strong>：</p><table tabindex="0"><thead><tr><th>检查项</th><th>差异</th><th>原因</th></tr></thead><tbody><tr><td>cmd_detail</td><td>13 vs 15</td><td>CMD_DETAIL 额外收录了流程内信号命令（--drift, --iteration）和 dev:help 自身</td></tr><tr><td>steps</td><td>知识库检查</td><td>步骤 0.5 仅存在于 mode-matrix.md，step-router.md 流程总览表不包含</td></tr><tr><td>steps</td><td>L3审查</td><td>help.md 使用简化名&quot;L3审查&quot;，router 使用全名&quot;L3 代码审查&quot;</td></tr><tr><td>triggers</td><td>刚和 XX 对齐 vs 对齐了</td><td>help.md 添加了口语化&quot;了&quot;字，不影响触发</td></tr></tbody></table><h2 id="其他规则" tabindex="-1">其他规则 <a class="header-anchor" href="#其他规则" aria-label="Permalink to &quot;其他规则&quot;">​</a></h2><ul><li><strong>Skill 优先级</strong>：用户级 Skill 优先于项目级同名 Skill</li><li><strong>dev-flow 优先级</strong>：「需求开发」= 写代码，文档类规则在 dev-flow 执行期间自动失效</li><li><strong>冲突时</strong>：dev-flow 胜出</li><li><strong>Skill 自动发现</strong>：步骤×Skill 匹配表 → <code>references/shared-rules.md</code> §5</li></ul><h2 id="关键红线-不可省略" tabindex="-1">关键红线（不可省略） <a class="header-anchor" href="#关键红线-不可省略" aria-label="Permalink to &quot;关键红线（不可省略）&quot;">​</a></h2><ul><li>❌ <strong>禁止自动 <code>git commit</code></strong>：commit message 仅生成，除非用户明确选择「📦 确认并提交」才执行</li><li>❌ <strong>必须调用 <code>smart-commit</code></strong>：禁止 AI 自行拼写 commit message（详见红牌 #12）</li><li>❌ <strong>dev-logs 目录命名四项 lint</strong>：步骤 4 创建 <code>~/.codebuddy/dev-logs/</code> 目录时，<code>name_lint</code> 四项（format_matched / type_valid / brief_has_chinese / no_project_suffix）必须全为 true，任一失败即 🔴 Block</li><li>❌ <strong>先搜索后编码</strong>：新增工具函数/组件/Hook 前必须先 <code>codebase_search</code> 确认无已有实现</li><li>❌ <strong>步骤完成必须经机器校验</strong>（v2 硬化 2026-04-30）：每个步骤完成标记 JSON 输出后，<strong>必须</strong>立即调用 <code>scripts/validate-output.sh &lt;step-id&gt; &lt;json-file&gt; &lt;flow-name&gt;</code> 脚本；脚本返回码非 0 或未触发 <code>.step-{N}.validated</code> 物理检查点 → 🔴 红牌 #14，禁止加载下一步骤。AI 不得自行 <code>touch</code> <code>.validated</code> 伪造通过。详见 <code>steps/step-router.md</code> §「🔐 物理检查点机制」和 <code>references/gate-validator.md</code> §「校验时机（v2 硬化版）」</li></ul>`,64)])])}const f=t(s,[["render",r]]);export{m as __pageData,f as default};

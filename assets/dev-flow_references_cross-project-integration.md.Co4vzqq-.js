import{_ as e,o as s,c as a,a2 as i}from"./chunks/framework.BUwdTBzS.js";const g=JSON.parse('{"title":"跨项目联调 · step-6C 联调 / 验证 / 本地并行","description":"","frontmatter":{"title":"跨项目联调 · step-6C 联调 / 验证 / 本地并行"},"headers":[],"relativePath":"dev-flow/references/cross-project-integration.md","filePath":"dev-flow/references/cross-project-integration.md","lastUpdated":null}'),o={name:"dev-flow/references/cross-project-integration.md"};function n(l,t,d,r,c,h){return s(),a("div",null,[...t[0]||(t[0]=[i(`<blockquote><p>📄 本页由源文件 <code>skills/dev-flow/references/cross-project/integration.md</code> 自动投影生成（单一权威源）。请勿直接编辑本页。</p></blockquote><h1 id="跨项目联调-·-step-6c-联调-验证-本地并行" tabindex="-1">跨项目联调 · step-6C 联调 / 验证 / 本地并行 <a class="header-anchor" href="#跨项目联调-·-step-6c-联调-验证-本地并行" aria-label="Permalink to &quot;跨项目联调 · step-6C 联调 / 验证 / 本地并行&quot;">​</a></h1><blockquote><p>父文件 → <code>references/cross-project-flow.md</code>。 本文件按需加载：B 项目 step-6C 命中跨项目 / A 项目验证回流 / 本地并行协作模式。</p></blockquote><h2 id="step-6c-扩展-跨项目联调" tabindex="-1">step-6C 扩展：跨项目联调 <a class="header-anchor" href="#step-6c-扩展-跨项目联调" aria-label="Permalink to &quot;step-6C 扩展：跨项目联调&quot;">​</a></h2><h3 id="触发条件" tabindex="-1">触发条件 <a class="header-anchor" href="#触发条件" aria-label="Permalink to &quot;触发条件&quot;">​</a></h3><p>step-6C 评估时，工作上下文包含 <code>cross_project.enabled: true</code> → 自动触发跨项目联调模式。</p><h3 id="扩展选项" tabindex="-1">扩展选项 <a class="header-anchor" href="#扩展选项" aria-label="Permalink to &quot;扩展选项&quot;">​</a></h3><table tabindex="0"><thead><tr><th>选项</th><th>说明</th></tr></thead><tbody><tr><td>🔗 跨项目联调</td><td>在验证项目中安装新版本并验证（生成回 A 项目的衔接 prompt）</td></tr><tr><td>⏭️ 跳过联调</td><td>不需要跨项目验证，直接进入步骤 7</td></tr><tr><td>📝 生成 Commit 并暂存</td><td>先提交 B 项目代码，暂存等待跨项目验证</td></tr></tbody></table><h3 id="选择「跨项目联调」后" tabindex="-1">选择「跨项目联调」后 <a class="header-anchor" href="#选择「跨项目联调」后" aria-label="Permalink to &quot;选择「跨项目联调」后&quot;">​</a></h3><ol><li><strong>更新状态</strong>：<code>cross_project.status</code> → <code>fixed_pending_validation</code></li><li><strong>生成 A 项目验证 prompt</strong>：</li></ol><div class="language-markdown vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">markdown</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">📋 </span><span style="--shiki-light:#24292E;--shiki-light-font-weight:bold;--shiki-dark:#E1E4E8;--shiki-dark-font-weight:bold;">**验证项目衔接 prompt**</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">（粘贴到 A 项目新对话中）：</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">&gt; dev-flow，跨项目验证衔接</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">&gt; </span><span style="--shiki-light:#24292E;--shiki-light-font-weight:bold;--shiki-dark:#E1E4E8;--shiki-dark-font-weight:bold;">**修复项目**</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">：{B 项目名}</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">&gt; </span><span style="--shiki-light:#24292E;--shiki-light-font-weight:bold;--shiki-dark:#E1E4E8;--shiki-dark-font-weight:bold;">**工作上下文**</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">：</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\`{B 项目工作上下文文件路径}\`</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">&gt; </span><span style="--shiki-light:#24292E;--shiki-light-font-weight:bold;--shiki-dark:#E1E4E8;--shiki-dark-font-weight:bold;">**修复内容**</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">：{一句话修复描述}</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">&gt; </span><span style="--shiki-light:#24292E;--shiki-light-font-weight:bold;--shiki-dark:#E1E4E8;--shiki-dark-font-weight:bold;">**安装命令**</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">：</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\`npm install {包名}@{新版本}\`</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;"> 或 </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\`npm link {本地路径}\`</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">&gt; </span><span style="--shiki-light:#24292E;--shiki-light-font-weight:bold;--shiki-dark:#E1E4E8;--shiki-dark-font-weight:bold;">**验证步骤**</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">：</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">&gt; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">1.</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;"> {验证步骤 1}</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">&gt; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">2.</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;"> {验证步骤 2}</span></span></code></pre></div><ol><li><strong>交互式选项</strong>：</li></ol><table tabindex="0"><thead><tr><th>选项</th><th>说明</th></tr></thead><tbody><tr><td>📋 复制验证 prompt</td><td>复制后去 A 项目验证，当前流程暂存于步骤 7</td></tr><tr><td>📦 先提交再验证</td><td>执行 commit 后再去验证</td></tr></tbody></table><h2 id="a-项目验证回流" tabindex="-1">A 项目验证回流 <a class="header-anchor" href="#a-项目验证回流" aria-label="Permalink to &quot;A 项目验证回流&quot;">​</a></h2><h3 id="识别信号" tabindex="-1">识别信号 <a class="header-anchor" href="#识别信号" aria-label="Permalink to &quot;识别信号&quot;">​</a></h3><p>A 项目新对话中检测到以下任一：</p><ul><li>包含&quot;跨项目验证衔接&quot;关键词</li><li>包含来自 B 项目的工作上下文路径</li></ul><h3 id="验证流程" tabindex="-1">验证流程 <a class="header-anchor" href="#验证流程" aria-label="Permalink to &quot;验证流程&quot;">​</a></h3><ol><li><strong>读取 B 项目工作上下文</strong>：提取修复内容和验证步骤</li><li><strong>安装新版本</strong>：提示用户执行安装命令（<strong>不自动执行</strong>）</li><li><strong>按验证步骤逐项验证</strong>：可选启动浏览器 MCP 自动化验证 / 用户手动验证后告知结果</li><li><strong>更新 B 项目工作上下文</strong>：<code>cross_project.status</code> → <code>validated</code> 或 <code>failed</code></li><li><strong>验证通过</strong>：提示用户回 B 项目完成收尾（commit/devlog）</li><li><strong>验证失败</strong>：记录失败原因，提示用户回 B 项目修复</li></ol><h2 id="知识库平台-消费方契约自动对齐" tabindex="-1">知识库平台 消费方契约自动对齐 <a class="header-anchor" href="#知识库平台-消费方契约自动对齐" aria-label="Permalink to &quot;知识库平台 消费方契约自动对齐&quot;">​</a></h2><blockquote><p>A 项目验证回流阶段，B 项目修改的导出符号与 A 项目实际调用之间，可能存在版本/签名/参数不对齐风险。</p></blockquote><h3 id="触发条件-2" tabindex="-1">触发条件（2） <a class="header-anchor" href="#触发条件-2" aria-label="Permalink to &quot;触发条件（2）&quot;">​</a></h3><p>A 项目收到「跨项目验证衔接」prompt <strong>且</strong> B 项目修改涉及导出 API（非纯改文案）时自动执行。</p><h3 id="执行步骤" tabindex="-1">执行步骤 <a class="header-anchor" href="#执行步骤" aria-label="Permalink to &quot;执行步骤&quot;">​</a></h3><ol><li>从 B 项目工作上下文提取本次修改的导出符号（函数名/类名/签名）</li><li>在 A 项目本地 <code>grep_search</code> 上述符号的实际调用点（导入 + 调用双检）</li><li>交叉对比：</li></ol><ul><li>新签名 vs A 项目调用处的实际传参 → 不匹配 → 🔴 红色告警</li><li>新签名 vs A 项目 TypeScript 定义 → 不匹配 → 提示运行 <code>tsc --noEmit</code></li></ul><ol><li>输出综合结论到验证报告</li></ol><h3 id="输出模板" tabindex="-1">输出模板 <a class="header-anchor" href="#输出模板" aria-label="Permalink to &quot;输出模板&quot;">​</a></h3><div class="language-markdown vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">markdown</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">🔍 契约对齐检测：{B 项目}→{A 项目}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">| 符号 | B 项目新签名 | A 项目调用 | 是否对齐 |</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">| --- | --- | --- | --- |</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">| isSameLink(a, b) | 无改动 | [</span><span style="--shiki-light:#032F62;--shiki-light-text-decoration:underline;--shiki-dark:#DBEDFF;--shiki-dark-text-decoration:underline;">Header/index.tsx#L42</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] 传参一致 | ✅ |</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">| 新增 fmtDate(v, opts?) | opts 可选 | 未调用 | ✅ |</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">| 修改 sign(s, key) → sign(s, { key }) | 对象语义 | [</span><span style="--shiki-light:#032F62;--shiki-light-text-decoration:underline;--shiki-dark:#DBEDFF;--shiki-dark-text-decoration:underline;">utils.ts#L18</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] 仍传位置参 | 🔴 不匹配 |</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">⚠️ 检测到 1 处契约不匹配，请在验证前调整 A 项目调用。</span></span></code></pre></div><h2 id="完整流转示例-issamelink-修复" tabindex="-1">完整流转示例（isSameLink 修复） <a class="header-anchor" href="#完整流转示例-issamelink-修复" aria-label="Permalink to &quot;完整流转示例（isSameLink 修复）&quot;">​</a></h2><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>🅰️ A 项目（发起方）</span></span>
<span class="line"><span>├── 步骤 0-1：发现问题，定位到 B 项目依赖包</span></span>
<span class="line"><span>├── 步骤 2：⚡ 检测到跨项目 → 标记 cross_project</span></span>
<span class="line"><span>│   └── 生成 B 项目衔接 prompt → 用户复制</span></span>
<span class="line"><span>│   └── .flow: status=paused, phase=integration</span></span>
<span class="line"><span>└── 暂存</span></span>
<span class="line"><span></span></span>
<span class="line"><span>🅱️ B 项目（修复方，新对话）</span></span>
<span class="line"><span>├── 粘贴衔接 prompt → read_file 共用工作上下文</span></span>
<span class="line"><span>├── 更新共用 .flow: status=active, phase=coding</span></span>
<span class="line"><span>├── 步骤 0：增量理解（从工作上下文继承）</span></span>
<span class="line"><span>├── 步骤 1：跳过（A 已完成研究）</span></span>
<span class="line"><span>├── 步骤 2~5：确认范围 → 制定方案 → 执行修改</span></span>
<span class="line"><span>├── 步骤 6C：⚡ 检测到跨项目 → 生成 A 验证 prompt</span></span>
<span class="line"><span>│   └── .flow: phase=integration, recovery=&quot;等 A 验证&quot;</span></span>
<span class="line"><span>└── 暂存（或先 commit 再暂存）</span></span>
<span class="line"><span></span></span>
<span class="line"><span>🅰️ A 项目（验证回流，新对话）</span></span>
<span class="line"><span>├── 粘贴验证 prompt → read_file 共用工作上下文</span></span>
<span class="line"><span>├── .flow: status=active, phase=integration</span></span>
<span class="line"><span>├── 安装新版本 → 逐项验证 → 通过</span></span>
<span class="line"><span>├── cross_project.status = validated</span></span>
<span class="line"><span>└── 提示回 B 项目完成收尾</span></span>
<span class="line"><span></span></span>
<span class="line"><span>🅱️ B 项目（收尾，新对话）</span></span>
<span class="line"><span>├── .flow: phase=commit-archive</span></span>
<span class="line"><span>└── 步骤 7 剩余环节（devlog + knowledge）→ 完成</span></span></code></pre></div><h2 id="边界情况处理" tabindex="-1">边界情况处理 <a class="header-anchor" href="#边界情况处理" aria-label="Permalink to &quot;边界情况处理&quot;">​</a></h2><table tabindex="0"><thead><tr><th>边界</th><th>处置</th></tr></thead><tbody><tr><td><strong>Monorepo</strong> A/B 同 workspace 不同 package</td><td>step-2 检测到文件在不同 package 但同 workspace；提示选「⏭️ 当前项目继续」；不生成衔接 prompt，直接当前对话处理</td></tr><tr><td><strong>B 项目工作区路径未知</strong></td><td>衔接 prompt 中 <code>fix_workspace</code> 留空；B 项目新对话由用户确认</td></tr><tr><td><strong>跨项目验证不通过</strong></td><td>记录失败原因到 A 项目工作上下文；提示用户回 B 触发迭代修复（<code>dev-flow 继续 xxx 修复</code>）；走增量步骤 1~3</td></tr><tr><td><strong>npm link vs npm install</strong></td><td>验证 prompt 同时提供两种：<code>npm link {本地路径}</code>（本地联调，无需发版）/ <code>npm install {包名}@{版本}</code>（B 已发版）</td></tr></tbody></table><h2 id="本地并行协作模式-同对话多仓库同时改动-2026-05-21-新增" tabindex="-1">本地并行协作模式（同对话多仓库同时改动，2026-05-21 新增） <a class="header-anchor" href="#本地并行协作模式-同对话多仓库同时改动-2026-05-21-新增" aria-label="Permalink to &quot;本地并行协作模式（同对话多仓库同时改动，2026-05-21 新增）&quot;">​</a></h2><blockquote><p><strong>适用</strong>：开发者本地同时有 A/B 两个（或更多）仓库改动，且<strong>同对话</strong>跨仓库执行编码、审查、提交。 与「三段式串行模式」并列存在。 <strong>典型案例</strong>：<code>business-project</code> + <code>component-lib</code> 同时改 / yalc/npm-link 联调后批量审查提交。</p></blockquote><h3 id="触发条件-任一" tabindex="-1">触发条件（任一） <a class="header-anchor" href="#触发条件-任一" aria-label="Permalink to &quot;触发条件（任一）&quot;">​</a></h3><ol><li><code>git status</code> 显示改动文件 + 用户明确提到&quot;另一个项目也改了&quot;</li><li>工作上下文 <code>cross_project.enabled: true</code> 且 <code>fix_workspace</code> 指向本地已知路径</li><li>当前对话中已对非当前 workspace 的仓库执行过 <code>read_file</code>/<code>replace_in_file</code> 等编辑</li><li>用户在编码阶段让 AI 同时在多个本地仓库执行修改</li></ol><h3 id="步骤同步规则-强制" tabindex="-1">步骤同步规则（强制） <a class="header-anchor" href="#步骤同步规则-强制" aria-label="Permalink to &quot;步骤同步规则（强制）&quot;">​</a></h3><p><strong>核心原则</strong>：<strong>步骤 5.5 / 清理 / commit</strong> 对每个有改动的仓库<strong>独立逐一执行</strong>，不可只处理主项目而遗漏协作项目。</p><table tabindex="0"><thead><tr><th>步骤</th><th>规则</th></tr></thead><tbody><tr><td><strong>步骤 5.5（L1 审查）</strong></td><td>每个仓库独立执行完整 8 项检查；ESLint 按各仓库自身配置跑</td></tr><tr><td><strong>调试日志清理</strong></td><td>每个仓库独立 <code>grep -rn -e &quot;console.log&quot; -e &quot;\\[DEBUG\\]&quot;</code> 验证；清理后必须再 <code>grep</code> 确认零命中</td></tr><tr><td><strong>Commit</strong></td><td>每个仓库独立生成 commit message；header 保持相同 任务平台 关联，body 注明跨项目配合关系</td></tr><tr><td><strong>Commit 顺序</strong></td><td>被依赖方（如组件库 B）先 commit，依赖方（业务项目 A）后 commit；保证 git 历史因果关系正确</td></tr><tr><td><strong>package.json 检查</strong></td><td>主项目 A 提交前检查依赖版本是否已恢复（去掉 yalc/link 调试改动）；本地联调改动（<code>file:.yalc/xxx</code>）<strong>禁止</strong>提交</td></tr></tbody></table><h3 id="commit-message-跨项目关联" tabindex="-1">Commit Message 跨项目关联 <a class="header-anchor" href="#commit-message-跨项目关联" aria-label="Permalink to &quot;Commit Message 跨项目关联&quot;">​</a></h3><ul><li><strong>B 项目（组件库）body 末尾</strong>：<code>[跨项目] 配合 {A 项目名} {需求简述}，需同步升级消费方依赖版本</code></li><li><strong>A 项目（业务项目）body 末尾</strong>：<code>[跨项目] 依赖 {B 包名}@{版本/分支}，组件库侧改动已同步提交</code></li></ul><h3 id="工作上下文记录" tabindex="-1">工作上下文记录 <a class="header-anchor" href="#工作上下文记录" aria-label="Permalink to &quot;工作上下文记录&quot;">​</a></h3><p><code>## 交付</code> 区块须列出所有仓库提交：</p><div class="language-markdown vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">markdown</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">## 交付</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">| 仓库 | 分支 | Commit | 状态 |</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">| --- | --- | --- | --- |</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">| component-lib | feature/xxx | feat: 列表支持个人用户展示模式 | ✅ 已提交 |</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">| business-project | feature/xxx | feat: 个人版分享弹窗支持列表 | ✅ 已提交 |</span></span></code></pre></div><h3 id="与三段式串行的判定边界" tabindex="-1">与三段式串行的判定边界 <a class="header-anchor" href="#与三段式串行的判定边界" aria-label="Permalink to &quot;与三段式串行的判定边界&quot;">​</a></h3><table tabindex="0"><thead><tr><th>条件</th><th style="text-align:center;">走并行协作</th><th style="text-align:center;">走三段式串行</th></tr></thead><tbody><tr><td>A/B 都在本地且同一开发者</td><td style="text-align:center;">✅</td><td style="text-align:center;">—</td></tr><tr><td>B 在本地但需开新对话才能操作</td><td style="text-align:center;">—</td><td style="text-align:center;">✅</td></tr><tr><td>B 不在本地（需 clone 或只有 知识库平台）</td><td style="text-align:center;">—</td><td style="text-align:center;">✅</td></tr><tr><td>不同开发者负责 A/B</td><td style="text-align:center;">—</td><td style="text-align:center;">✅</td></tr><tr><td>同一对话已对 A/B 都做了编辑</td><td style="text-align:center;">✅</td><td style="text-align:center;">—</td></tr></tbody></table><h3 id="反模式-违反即拒收" tabindex="-1">反模式（违反即拒收） <a class="header-anchor" href="#反模式-违反即拒收" aria-label="Permalink to &quot;反模式（违反即拒收）&quot;">​</a></h3><ul><li>❌ 只对主项目跑 L1 审查，遗漏协作项目调试日志/ESLint</li><li>❌ 主项目提交时未检查 package.json 是否含本地联调改动（yalc/link）</li><li>❌ 两仓库 commit 没有体现关联性</li><li>❌ 步骤 5.5 只跑了一次、对一个仓库的改动文件就宣告完成</li><li>❌ 依赖方先 commit、被依赖方后 commit（因果倒置）</li></ul><h2 id="diff-commit-查看规范-跨项目改动确认" tabindex="-1">Diff / Commit 查看规范（跨项目改动确认） <a class="header-anchor" href="#diff-commit-查看规范-跨项目改动确认" aria-label="Permalink to &quot;Diff / Commit 查看规范（跨项目改动确认）&quot;">​</a></h2><blockquote><p><strong>背景</strong>：feature 分支通常含从 master 合入的他人改动。<code>git diff merge-base..HEAD</code> 会把这些都算进去导致误判。<code>git log</code> 不加 <code>--author</code> 也会混入他人 commit。 <strong>适用</strong>：收尾对账（步骤 7 环节 A）、跨项目验证（step-6C）、需求范围确认（step-2）。</p></blockquote><h3 id="强制顺序" tabindex="-1">强制顺序 <a class="header-anchor" href="#强制顺序" aria-label="Permalink to &quot;强制顺序&quot;">​</a></h3><ol><li><strong>确认当前分支</strong>：<code>git branch --show-current</code> → 必须与需求分支一致；不一致则<strong>停止操作并提醒用户</strong>（禁止擅自 checkout）</li><li><strong>查看 diff 时区分&quot;本人&quot;与&quot;合入他人&quot;</strong>（按场景三选一）：</li></ol><ul><li><strong>场景 1（默认）· 按作者过滤</strong>：<code>git --no-pager log --oneline --author=&quot;{用户名}&quot; $(git merge-base origin/master HEAD)..HEAD</code> 列出本人 commit，再逐个 <code>git --no-pager show &lt;hash&gt; --stat</code> 查看</li><li><strong>场景 2 · 多人协作 / squash merge 后追溯 · 按 任务平台 单据过滤</strong>：<code>git --no-pager log --oneline --grep=&quot;--story={task_id}&quot; $(git merge-base origin/master HEAD)..HEAD</code> 聚合同一需求的所有 commit（覆盖跨开发者、跨分支、squash 后丢失作者信息的场景）；任务平台 bug 用 <code>--grep=&quot;--bug={id}&quot;</code>，task 用 <code>--grep=&quot;--task={id}&quot;</code></li><li><strong>场景 3 · 长周期需求全量对账（多次迭代修复后）· 双重过滤</strong>：先按 <code>--author</code> + <code>--grep=&quot;--story=&quot;</code> 双条件交叉验证 commit 完整性（<code>git log --author=&quot;{用户名}&quot; --grep=&quot;--story={task_id}&quot;</code>），再用 <code>git --no-pager diff $(git merge-base origin/master HEAD)..HEAD</code> 看完整累积改动，配合步骤 7 环节 A · §H.3+ 三方对账兜底</li><li>⚠️ <code>git diff merge-base..HEAD</code> 结果<strong>可能含他人改动</strong>，不能直接作为需求范围结论</li></ul><ol><li><strong>查看 commit 必须过滤作者或 任务平台 单据</strong>：默认加 <code>--author=&quot;{用户名}&quot;</code>；多人协作 / squash merge 场景加 <code>--grep=&quot;--story={id}&quot;</code>；禁止两者都不加就断言&quot;该分支有/没有某改动&quot;</li></ol><h3 id="反模式-违反即拒收-2" tabindex="-1">反模式（违反即拒收）（2） <a class="header-anchor" href="#反模式-违反即拒收-2" aria-label="Permalink to &quot;反模式（违反即拒收）（2）&quot;">​</a></h3><ul><li>❌ 不确认分支就执行 <code>git diff</code></li><li>❌ 用 <code>git diff merge-base..HEAD --name-only</code> 直接当作&quot;本需求改动文件清单&quot;</li><li>❌ 不加 <code>--author</code> 看 <code>git log</code> 就说&quot;该项目 commit 内容是 XXX&quot;</li><li>❌ 因 <code>git diff merge-base..HEAD</code> 与预期不符就得出&quot;该项目未做改动&quot;（应进一步用 <code>git show &lt;hash&gt;</code> 确认本人 commit 实际内容）</li><li>❌ 多人协作 feature 分支 / squash merge 后追溯场景，仅用 <code>--author</code> 过滤就断言需求范围（会漏掉队友为同一 story 提交的代码、漏掉 squash 后丢失作者信息的 commit）；必须叠加 <code>--grep=&quot;--story={id}&quot;</code> 按 任务平台 单据聚合</li><li>❌ 长周期需求（多次迭代修复后）只看最近一次 <code>git diff</code> 就汇报完成；必须按场景 3 双重过滤交叉验证 + §H.3+ 三方对账兜底</li></ul><h3 id="统计结果回写-步骤-7-9a-度量采集时" tabindex="-1">统计结果回写（步骤 7 / 9a 度量采集时） <a class="header-anchor" href="#统计结果回写-步骤-7-9a-度量采集时" aria-label="Permalink to &quot;统计结果回写（步骤 7 / 9a 度量采集时）&quot;">​</a></h3><p>逐项目完成 diff 统计后，必须回写主项目工作上下文 <code>cross_project.projects_detail</code>（每项目 <code>branch</code> / <code>files_changed</code> / <code>lines_added</code> / <code>lines_deleted</code> / <code>mr</code> / <code>mr_url</code> / <code>mr_status</code>，schema 见 <code>templates/working-context.tpl.md</code>）——这是复盘报告「涉及项目」链路图的唯一数据源；未回写则报告节点只显示项目名与角色，无分支/规模/MR 信息。迭代修复轮次中改动有变化时同步更新。</p><h2 id="历史相似改动反查规范-问题驱动" tabindex="-1">历史相似改动反查规范（问题驱动） <a class="header-anchor" href="#历史相似改动反查规范-问题驱动" aria-label="Permalink to &quot;历史相似改动反查规范（问题驱动）&quot;">​</a></h2><blockquote><p><strong>背景</strong>：dev-flow 步骤 1（研究）/ 步骤 5（编码定位）期间，发现某段代码可疑（如疑似缺可选链、疑似竞态、疑似空 catch），需要回溯<strong>历史需求里有没有相同模式的坑</strong>——以判断本次是孤例还是系统性遗漏，并避免遗漏同类隐患。 与上一节「Diff / Commit 查看规范」的边界：上一节是<strong>收尾对账 / 改动确认</strong>（&quot;我改了什么&quot;），本节是<strong>问题驱动反查</strong>（&quot;历史改动里有没有相同模式的坑&quot;），命令栈与意图均不同。 <strong>适用</strong>：步骤 1 研究阶段验证某代码模式的历史风险面 / 步骤 5 编码时发现可疑模式需举一反三 / issue-trace 中怀疑 bug 由历史需求引入时。</p></blockquote><h3 id="命令栈-按优先级" tabindex="-1">命令栈（按优先级） <a class="header-anchor" href="#命令栈-按优先级" aria-label="Permalink to &quot;命令栈（按优先级）&quot;">​</a></h3><ol><li><strong>按字面量回溯（首选 · 准确率最高）</strong> · <code>git log -S</code></li></ol><ul><li>命令：<code>git --no-pager log -S&quot;{字面量}&quot; --all --oneline --source</code></li><li>用途：找出<strong>新增或删除</strong>了该字面量的所有 commit（pickaxe 模式）。如查&quot;哪些 commit 引入了 <code>order_simple_info.order_code</code> 的裸链式访问&quot;</li><li>加 <code>--all</code> 可跨分支搜索；加 <code>-p</code> 看具体 diff；加 <code>--follow {file}</code> 跟踪文件改名</li></ul><ol><li><strong>按正则模式回溯</strong> · <code>git log -G</code></li></ol><ul><li>命令：<code>git --no-pager log -G&#39;{正则}&#39; --all --oneline</code>（用单引号避免 shell 转义干扰）</li><li>用途：字面量不固定时按模式搜。<strong>注意 <code>-G</code> 只匹配 diff 中&quot;新增/删除行&quot;包含该模式的 commit，不匹配代码当前状态</strong>——所以正则要尽量贴近&quot;曾经被增删过的具体行的关键特征&quot;</li><li>推荐示例（按由稳到激进排序）：</li><li><code>git log -G&#39;catch\\s*\\(\\s*\\w+\\s*\\)\\s*\\{\\s*\\}&#39; --all --oneline</code> 找空 catch 引入历史</li><li><code>git log -G&#39;\\bvar\\s+\\w+&#39; --all --oneline -- &#39;*.ts&#39; &#39;*.tsx&#39;</code> 找 var 残留引入</li><li><code>git log -G&#39;==\\s*null&#39; --all --oneline</code> 找弱等判空</li><li><strong>不推荐</strong>形如 <code>\\?\\.\\w+\\?\\.\\w+\\?\\.length</code> 的多层链式裸正则——历史 commit 同时增删三层链式的概率极低，常出空结果；这种场景改用：先 <code>grep -rn &#39;?\\..*?\\..*?\\.length&#39; src/</code> 找当前代码命中位置 → 再对每处 <code>git blame -L</code> 回溯（见命令 3）</li></ul><ol><li><strong>精确定位问题行修改人</strong> · <code>git blame -L</code></li></ol><ul><li>命令：<code>git --no-pager blame -L {起始行},{结束行} -- {文件路径}</code></li><li>用途：确认<strong>当前可疑行</strong>最后由哪个 commit 引入；与 issue-trace 相关模块共用同一规范，但本节关注&quot;该 commit 是哪个需求引入的&quot;</li><li>⚠️ 必须 <code>-L</code> 精确到问题行，禁止用 <code>git log {file}</code> 当替代（前者定位行，后者定位文件最后修改）</li></ul><ol><li><strong>关联到 任务平台 需求单（用于判定&quot;哪个需求引入了这个坑&quot;）</strong></li></ol><ul><li>命令：<code>git --no-pager log -S&quot;{字面量}&quot; --all --grep=&quot;--story=\\|--bug=\\|--task=&quot; --oneline</code></li><li>或：拿到嫌疑 commit 后 <code>git --no-pager show {hash} | head -20</code> 看 commit message 中的 <code>--story=xxx</code> / <code>--bug=xxx</code></li><li>用途：把代码模式 → commit → 任务平台 单据串联，便于在 任务平台 上反查需求背景，决定是否需要顺手举一反三修同类坑</li></ul><h3 id="命令选用决策" tabindex="-1">命令选用决策 <a class="header-anchor" href="#命令选用决策" aria-label="Permalink to &quot;命令选用决策&quot;">​</a></h3><table tabindex="0"><thead><tr><th>场景</th><th>首选命令</th><th>备选</th></tr></thead><tbody><tr><td>知道具体的可疑字符串（变量名/字段名/字面量）</td><td><code>git log -S</code></td><td><code>git log -G</code>（带正则边界）</td></tr><tr><td>只能用模式描述（&quot;裸链式 length 访问&quot;）</td><td><code>git log -G</code></td><td>grep 当前代码 + <code>git blame -L</code> 逐个回溯</td></tr><tr><td>怀疑当前某行是历史 bug</td><td><code>git blame -L</code></td><td>拿到 commit 后再 <code>git log -S</code> 扩展同类</td></tr><tr><td>想知道&quot;这个反模式还在哪些需求里出现过&quot;</td><td><code>git log -S</code> 或 <code>-G</code> 后 <code>--grep</code> 提取 story 列表</td><td>结合 <code>git_merge_request</code> 知识库平台 查 MR 评审历史</td></tr></tbody></table><h3 id="反模式-违反即拒收-3" tabindex="-1">反模式（违反即拒收）（3） <a class="header-anchor" href="#反模式-违反即拒收-3" aria-label="Permalink to &quot;反模式（违反即拒收）（3）&quot;">​</a></h3><ul><li>❌ 用 <code>git log {file}</code> 替代 <code>git blame -L {start},{end}</code>——前者只能定位文件最后修改者，无法精确到问题行的引入 commit</li><li>❌ 用 <code>grep -rn</code> 当前代码后就断言&quot;历史也是这么写的&quot;——当前代码只是切片，必须用 <code>git log -S/-G</code> 看历史增删轨迹</li><li>❌ 找到一个嫌疑 commit 就停手——必须用 <code>--all</code> 跨分支、用 <code>-S/-G</code> 列全量同模式 commit，避免漏掉合入主干前的 feature 分支历史</li><li>❌ 不区分&quot;问题驱动反查&quot;与&quot;收尾对账&quot;——前者用 <code>-S/-G/blame -L</code>，后者用上一节的 <code>--author/--grep=&quot;--story=&quot;</code>；混用会得出错误结论</li><li>❌ 反查到历史同类坑后<strong>擅自顺手修复</strong>——按红线 #2 最小入侵：发现范围外同类问题 → 提及但不擅自修改，告知用户后由用户决定是否新开 dev-flow 处理</li></ul><h3 id="与其他-skill-的协作" tabindex="-1">与其他 skill 的协作 <a class="header-anchor" href="#与其他-skill-的协作" aria-label="Permalink to &quot;与其他 skill 的协作&quot;">​</a></h3><table tabindex="0"><thead><tr><th>来源场景</th><th>入口</th><th>进入本节的方式</th></tr></thead><tbody><tr><td>dev-flow 步骤 1 研究阶段，需要评估某代码模式的历史风险面</td><td>步骤 1 研究</td><td>加载本节命令栈</td></tr><tr><td>dev-flow 步骤 5 编码时发现可疑模式，要决定是否扩大修复范围</td><td>步骤 5 编码</td><td>反查后回到红线 #2 决策（不擅自扩范围，告知用户）</td></tr><tr><td>issue-trace 纯分析阶段沿调用链回溯根因</td><td>issue-trace 主流程</td><td>拿到可疑代码切片后用本节命令栈反查历史改动轨迹</td></tr></tbody></table>`,76)])])}const k=e(o,[["render",n]]);export{g as __pageData,k as default};
